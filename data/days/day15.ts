import type { Day } from '../types';

const day: Day = {
  day: 15,
  title: 'Databases',
  goal: 'Ask a database a question and read the answer with confidence.',
  concepts: [
    { id: 'd15-c01', term: 'Database', plain: 'A database is an organised store of data that survives restarts and serves many users.', why: 'Files are fine for one person. A database handles thousands of readers and writers without losing or corrupting data.', example: 'A shop keeps customers, products and orders in a database that stays correct when two tills sell at once.', check: { q: 'What does a database offer that a plain file does not?', options: ['Safe access by many users at once', 'Faster typing', 'Smaller storage'], answer: 0, explain: 'A database coordinates many readers and writers and keeps data consistent under load.' } },
    { id: 'd15-c02', term: 'Table', plain: 'A table stores one kind of thing as rows and columns.', why: 'Most business data fits in tables. Thinking in tables is the first step to asking data questions.', example: 'A customers table has one row per customer and columns for name, email and city.', check: { q: 'What does a table hold?', options: ['Pictures', 'A single number', 'One kind of thing as rows and columns'], answer: 2, explain: 'A table is a grid. Each row is one item, each column one attribute.' } },
    { id: 'd15-c03', term: 'Row and column', plain: 'A row is one record. A column is one attribute shared by every row.', why: 'Rows are the things you count. Columns are the things you filter and measure.', example: 'In the orders table, one row is one order, and the amount column holds its price.', check: { q: 'In a table of orders, what is one row?', options: ['One column of prices', 'One order', 'The whole table'], answer: 1, explain: 'A row is a single record. Here that is a single order.' } },
    { id: 'd15-c04', term: 'Primary key', plain: 'A primary key is a column whose value is unique for every row.', why: 'Without a unique id you cannot point at one row safely. Names repeat, ids do not.', example: 'Customer 1042 is one person even if two customers are called Sam Lee.', check: { q: 'Which column makes a good primary key?', options: ['Surname', 'City', 'A unique customer id'], answer: 2, explain: 'Surnames and cities repeat. A unique id identifies exactly one row.' } },
    { id: 'd15-c05', term: 'Schema', plain: 'A schema is the declared shape of the tables, their columns and their types.', why: 'A schema catches bad data on the way in. Text in a price column fails loudly rather than silently.', example: 'orders has id integer, customer_id integer, amount decimal, placed_at timestamp.', check: { q: 'What does a schema define?', options: ['The tables, columns and types', 'The number of users', 'The server location'], answer: 0, explain: 'The schema is the structure. It says what may be stored and how.' } },
    { id: 'd15-c06', term: 'SELECT query', plain: 'A query asks the database for rows. SELECT names columns and FROM names the table.', why: 'SQL is the language of data questions. Every analyst, engineer and model pipeline reads it.', example: 'SELECT name, city FROM customers returns two columns for every customer.', check: { q: 'What does FROM name in a query?', options: ['The columns to return', 'The table to read', 'The order of results'], answer: 1, explain: 'FROM points at the table. SELECT picks the columns from it.' } },
    { id: 'd15-c07', term: 'WHERE filter', plain: 'WHERE keeps only the rows that match a condition.', why: 'Almost every question is about a subset. Orders last week, users in Leeds, errors above a threshold.', example: 'SELECT * FROM orders WHERE amount > 100 returns only the large orders.', check: { q: 'What does WHERE do?', options: ['Sorts the rows', 'Deletes the table', 'Keeps rows matching a condition'], answer: 2, explain: 'WHERE is a filter. Rows that fail the test are left out of the result.' } },
    { id: 'd15-c08', term: 'Aggregation', plain: 'Aggregation squashes many rows into one number, such as a count, sum or average.', why: 'Leaders want totals, not rows. GROUP BY gives one total per category.', example: 'SELECT city, COUNT(*) FROM customers GROUP BY city counts customers in each city.', check: { q: 'What does GROUP BY change about an aggregate?', options: ['It gives one result per group', 'It makes the query faster', 'It removes duplicates only'], answer: 0, explain: 'Without GROUP BY you get one total. With it you get one total for each group value.' } },
    { id: 'd15-c09', term: 'Foreign key', plain: 'A foreign key is a column that holds the primary key of a row in another table.', why: 'Foreign keys link tables. An order knows its customer by storing the customer id.', example: 'orders.customer_id is 1042, which points at the customer row with id 1042.', check: { q: 'Where does the link from an order to its customer live?', options: ['In a customer_id column on orders', 'In the customers table', 'In a separate file'], answer: 0, explain: 'The order row stores the customer id. That column is the foreign key.' } },
    { id: 'd15-c10', term: 'Join', plain: 'A join combines rows from two tables where their keys match.', why: 'Data is split across tables to avoid repetition. Joins put the pieces back together for a question.', example: 'Joining orders to customers on customer_id gives each order with its customer name.', check: { q: 'What decides which rows an inner join pairs up?', options: ['Row position', 'Matching key values', 'Alphabetical order'], answer: 1, explain: 'An inner join pairs rows whose join keys are equal. Unmatched rows are dropped.' } },
    { id: 'd15-c11', term: 'Index', plain: 'An index is a sorted lookup structure that finds rows without scanning the whole table.', why: 'Without an index a search reads every row. With one it reads a handful. That is milliseconds against minutes.', example: 'An index on email finds one customer in a million rows in about 20 steps.', check: { q: 'What is the cost of adding an index?', options: ['Reads get slower', 'The table must be rebuilt daily', 'Writes get slower and storage grows'], answer: 2, explain: 'Every insert or update must also update the index. Reads get faster, writes pay for it.' } },
    { id: 'd15-c12', term: 'Transaction', plain: 'A transaction groups changes so that all of them happen or none of them do.', why: 'Moving money means one debit and one credit. If the server dies between them, the transaction rolls both back.', example: 'BEGIN, subtract 50 from account A, add 50 to account B, COMMIT.', check: { q: 'A server crashes halfway through a transaction. What happens?', options: ['Half the changes stay', 'All the changes are undone', 'The database is corrupted'], answer: 1, explain: 'A transaction is all or nothing. On crash the partial work is rolled back.' } },
    { id: 'd15-c13', term: 'Normalisation', plain: 'Normalisation stores each fact once, in one table, and links to it from elsewhere.', why: 'Repeating a customer address on every order means updating it in a hundred places. One copy means one update.', example: 'Keep the address on customers only. Orders store the customer id, not the address.', check: { q: 'Why avoid copying the customer address onto every order?', options: ['Addresses are too long', 'Joins are not allowed', 'Updates would need many changes'], answer: 2, explain: 'One fact in one place. A change updates a single row and nothing goes stale.' } },
    { id: 'd15-c14', term: 'Document store', plain: 'A document store keeps flexible records, often JSON, without a fixed schema.', why: 'Fast-changing data like model outputs or event logs fits documents better than rigid tables.', example: 'MongoDB stores a chat turn as one document with prompt, response and metadata fields.', check: { q: 'When does a document store fit better than tables?', options: ['When records vary in shape', 'When you need heavy joins', 'When data never changes'], answer: 0, explain: 'Documents suit records with different fields. Tables suit fixed shapes and joins.' } },
    { id: 'd15-c15', term: 'Replication', plain: 'Replication copies data to other servers for safety and for extra read capacity.', why: 'One server can fail or be swamped. Replicas take over and serve reads, but copies can lag behind.', example: 'A write goes to the primary. A replica shows it 50 milliseconds later.', check: { q: 'A user saves a setting, reads it from a replica and sees the old value. Why?', options: ['The write was rejected', 'Replication lag', 'The index is missing'], answer: 1, explain: 'Replicas copy changes with a delay. Read from the primary when you must see your own write.' } },
  ],
  equations: [
    { id: 'd15-e01', name: 'Cross join rows', symbol: 'R = n × m', tokens: [{ sym: 'R', word: 'rows out' }, { sym: '=', word: 'is' }, { sym: 'n', word: 'left rows' }, { sym: '×', word: 'times' }, { sym: 'm', word: 'right rows' }], literal: 'rows out is left rows times right rows', meaning: 'A join with no matching condition pairs every left row with every right row. This is why a missing join condition explodes.', example: '4 customers cross joined with 3 products gives 12 rows.', check: { q: 'Left table has 5 rows, right has 4. Cross join rows?', options: ['9', '20', '1'], answer: 1, explain: '5 times 4 is 20. Every left row pairs with every right row.' } },
    { id: 'd15-e02', name: 'Inner join rows', symbol: 'R = Σ mᵢ', tokens: [{ sym: 'R', word: 'rows out' }, { sym: '=', word: 'is' }, { sym: 'Σ', word: 'add-up' }, { sym: 'mᵢ', word: 'matches per left row' }], literal: 'rows out is add-up of matches per left row', meaning: 'Each left row appears once for every right row that matches its key. Rows with no match vanish from an inner join.', example: '3 customers with 2, 0 and 5 orders give 7 rows when joined to orders.', check: { q: 'Left rows have 3, 0 and 2 matches. Inner join rows?', options: ['5', '3', '6'], answer: 0, explain: 'Add up the matches. 3 plus 0 plus 2 is 5 rows out.' } },
    { id: 'd15-e03', name: 'Rows after filter', symbol: 'k = N × s', tokens: [{ sym: 'k', word: 'rows kept' }, { sym: '=', word: 'is' }, { sym: 'N', word: 'total rows' }, { sym: '×', word: 'times' }, { sym: 's', word: 'matching fraction' }], literal: 'rows kept is total rows times matching fraction', meaning: 'A WHERE clause keeps a fraction of the table. The planner uses this estimate to decide whether an index is worth using.', example: '10000 orders, 5 percent over 100 pounds, so about 500 rows kept.', check: { q: '20000 rows, 10 percent match the filter. Rows kept?', options: ['200', '20', '2000'], answer: 2, explain: '20000 times 0.1 is 2000 rows.' } },
    { id: 'd15-e04', name: 'Index lookup steps', symbol: 'steps ≈ log₂ N', tokens: [{ sym: 'steps', word: 'steps' }, { sym: '≈', word: 'is approximately' }, { sym: 'log₂', word: 'log base two of' }, { sym: 'N', word: 'total rows' }], literal: 'steps is approximately log base two of total rows', meaning: 'A sorted index halves the search space each step. Doubling the table adds one step, not double the work.', example: 'One million rows need about 20 steps with an index, or one million without.', check: { q: 'About how many steps to find a row among one million with an index?', options: ['1000000', '20', '500000'], answer: 1, explain: 'Two to the power 20 is about a million, so around 20 halving steps.' } },
    { id: 'd15-e05', name: 'Storage size', symbol: 'S = N × r', tokens: [{ sym: 'S', word: 'storage size' }, { sym: '=', word: 'is' }, { sym: 'N', word: 'total rows' }, { sym: '×', word: 'times' }, { sym: 'r', word: 'bytes per row' }], literal: 'storage size is total rows times bytes per row', meaning: 'Estimate disk and memory before loading data. Indexes add more on top, often as much again.', example: '1 million rows at 200 bytes each is about 200 megabytes before indexes.', check: { q: '2 million rows at 100 bytes each. Storage?', options: ['200 megabytes', '2 gigabytes', '20 megabytes'], answer: 0, explain: '2 million times 100 is 200 million bytes, about 200 megabytes.' } },
  ],
  problems: [
    { id: 'd15-p01', prompt: 'Customers A, B and C have 2, 0 and 4 orders. How many rows does an inner join of customers to orders return?', hint: 'Add up the matches for each customer.', options: ['6', '3', '12'], answer: 0, worked: '2 plus 0 plus 4 is 6. Customer B has no orders, so B does not appear at all.' },
    { id: 'd15-p02', prompt: 'A lookup on an unindexed table of one million rows reads every row. With an index, roughly how many steps does it take?', hint: 'Halve one million until you reach one.', options: ['1000', '20', '1000000'], answer: 1, worked: 'Halving a million twenty times reaches one. The index turns a million reads into about 20.' },
  ],
  build: { id: 'd15-b01', title: 'SQLite order book', goal: 'Create two linked tables in Python with sqlite3 and answer a question with a join.', steps: ['Open a file with sqlite3.connect and create customers and orders tables.', 'Insert three customers and six orders that reference customer ids.', 'Run a join grouped by customer that counts orders per customer.', 'Print the result and confirm the totals by hand.'], done: 'A join query prints each customer name with the right order count.', check: { q: 'Which table holds the customer id as a foreign key?', options: ['Orders', 'Customers', 'Both equally'], answer: 0, explain: 'Each order points at its customer. The customers table does not need to know its orders.' } },
  incident: { id: 'd15-i01', title: 'Database slow every night at two', signal: 'Query latency rises from 5 milliseconds to 3 seconds every night at 2am for twenty minutes, then recovers. No deploys happened.', clue: 'A new nightly report filters a 50 million row table on a column with no index.', options: ['The disk has run out of space', 'A network outage at the data centre', 'A full table scan from the nightly report'], answer: 2, explain: 'The report scans every row for twenty minutes and starves other queries. Add an index on the filtered column, or run the report on a replica.' },
  research: { id: 'd15-r01', question: 'Does adding indexes always speed up a workload that mixes reads and writes?', hypothesis: 'Once writes exceed half of operations, each extra index slows the workload overall.', method: 'Load one table with a million rows. Run fixed read and write mixes with zero, two and five indexes and time each run.', check: { q: 'Why include a run with zero indexes?', options: ['Indexes are always harmful', 'It gives a baseline to compare against', 'It is faster to set up'], answer: 1, explain: 'Without a baseline you cannot say whether an index helped or hurt.' } },
  decision: { id: 'd15-l01', situation: 'Your team wants to store every prompt and response from a new assistant. One engineer proposes writing them straight into the production user database. Traffic is 500 requests per second.', options: ['Write logs into the production user database', 'Write to a separate append-only store', 'Do not store prompts at all'], answer: 1, reasoning: 'Heavy writes would compete with user reads and risk the core service. A separate append-only store scales cheaply and keeps failures apart. Not storing loses evaluation and audit data.' },
};

export default day;
