import type { Day } from '../types';

const day: Day = {
  day: 76,
  title: 'Architecture',
  goal: 'Draw an AI system as stages, each with a latency, cost and failure budget.',
  concepts: [
    { id: 'd76-c01', term: 'Component', plain: 'A component is one box in the system that does one job.', why: 'Drawing the system as boxes lets you reason about cost, latency and failure one piece at a time instead of all at once.', example: 'Gateway, orchestrator, retriever, model, cache. Five boxes, five jobs.', check: { q: 'What is a component?', options: ['A line of code', 'One box with one job', 'The whole product'], answer: 1, explain: 'Components split a system into parts you can budget, test and replace separately.' } },
    { id: 'd76-c02', term: 'Interface', plain: 'An interface is the agreed shape of what passes between two components.', why: 'If the shape is fixed, either side can be rebuilt or replaced without breaking the other. That is what makes build or buy possible later.', example: 'The retriever takes a query string and returns at most ten passages with scores.', check: { q: 'What does a fixed interface allow?', options: ['Replacing one side without breaking the other', 'Skipping tests', 'Faster models'], answer: 0, explain: 'With the shape fixed, the two sides only depend on the contract.' } },
    { id: 'd76-c03', term: 'Request path', plain: 'The request path is the ordered list of components one request touches.', why: 'Latency, cost and risk all add up along the path. If you cannot draw it, you cannot budget it.', example: 'User to gateway, to orchestrator, to retriever, to model, back through the orchestrator to the user.', check: { q: 'Why draw the request path?', options: ['To choose a colour scheme', 'To impress the board', 'Because cost and latency add up along it'], answer: 2, explain: 'Every stage on the path adds time and money. The drawing is the budget.' } },
    { id: 'd76-c04', term: 'Gateway', plain: 'A gateway is the single entry point that authenticates, rate limits and logs every request.', why: 'One door means one place to check identity, stop floods and record what happened. Many doors means one is always unlocked.', example: 'Every client calls the gateway with a key. Nothing else is reachable from outside.', check: { q: 'What is the main benefit of one gateway?', options: ['Lower model cost', 'One place to authenticate, limit and log', 'Faster retrieval'], answer: 1, explain: 'A single entry point concentrates the controls that every request needs.' } },
    { id: 'd76-c05', term: 'Orchestrator', plain: 'The orchestrator decides which steps run for a request and in what order.', why: 'Keeping the decision logic in one place stops it leaking into prompts and clients. It is also where retries and timeouts live.', example: 'If the query mentions an order number, fetch the order first, then call the model.', check: { q: 'Where should the retry and timeout logic live?', options: ['In the orchestrator', 'In the user interface', 'Inside the model prompt'], answer: 0, explain: 'The orchestrator sees every step. It is the only place that can retry or abandon one.' } },
    { id: 'd76-c06', term: 'Retrieval layer', plain: 'The retrieval layer finds the passages the model needs, from a vector index or a database.', why: 'Retrieval is usually the second largest cost and the first cause of wrong answers. It deserves its own box and its own budget.', example: 'Embed the query, search the index, return the top eight passages with sources.', check: { q: 'Why give retrieval its own component?', options: ['It never fails', 'It is free', 'It has its own cost, latency and failures'], answer: 2, explain: 'Separate boxes get separate budgets and separate monitoring.' } },
    { id: 'd76-c07', term: 'Model layer', plain: 'The model layer runs the language model, hosted by you or rented through an API.', why: 'It is the slowest and most expensive stage. Everything else in the design exists to feed it well and call it rarely.', example: 'A hosted API at 600 milliseconds per call, or a self hosted model on two GPUs.', check: { q: 'Which stage usually dominates cost and latency?', options: ['The gateway', 'The model layer', 'The cache'], answer: 1, explain: 'Model calls cost far more than any other stage. Design to reduce them.' } },
    { id: 'd76-c08', term: 'Cache', plain: 'A cache stores earlier answers so repeated requests skip the expensive stages.', why: 'A cache hit costs nearly nothing. Even a modest hit rate cuts both cost and latency because it removes model calls.', example: 'The same product question asked 500 times a day is answered once by the model and 499 times by the cache.', check: { q: 'What does a cache hit skip?', options: ['The expensive model call', 'The gateway', 'Logging'], answer: 0, explain: 'A hit returns a stored answer. The model is never called.' } },
    { id: 'd76-c09', term: 'Queue', plain: 'A queue holds work so slow steps run in the background without blocking the user.', why: 'Not every step needs an answer now. Indexing, summarising and reporting belong on a queue, not on the request path.', example: 'A new document is queued for embedding. The user gets an acknowledgement in ten milliseconds.', check: { q: 'Which task belongs on a queue rather than the request path?', options: ['Checking the user key', 'Returning the answer', 'Embedding a newly uploaded document'], answer: 2, explain: 'The user does not need to wait for indexing. Queue it and acknowledge.' } },
    { id: 'd76-c10', term: 'State store', plain: 'A state store keeps conversation history and job status outside the model.', why: 'Models forget between calls. If state lives only in a prompt, it is lost on restart and impossible to audit.', example: 'Each conversation is a row in Postgres with its messages, user id and last update time.', check: { q: 'Where should conversation history live?', options: ['Only in the prompt', 'In a database the orchestrator reads', 'In the model weights'], answer: 1, explain: 'A store survives restarts and can be inspected, audited and deleted.' } },
    { id: 'd76-c11', term: 'Latency budget', plain: 'A latency budget assigns each stage a share of the total time the user will accept.', why: 'Without a budget, every team believes its stage is allowed the whole time. With one, overruns are visible and owned.', example: 'Total 800 milliseconds. Gateway 20, retrieval 80, model 600, formatting 50. Fifty spare.', check: { q: 'What does a latency budget give each stage?', options: ['A share of the total allowed time', 'A separate server', 'A cost limit'], answer: 0, explain: 'Each stage gets a time allowance. The sum must stay under the user limit.' } },
    { id: 'd76-c12', term: 'Critical path', plain: 'The critical path is the longest chain of stages that must run one after another.', why: 'Only the critical path sets total latency. Speeding up a stage that runs in parallel to a slower one changes nothing.', example: 'Retrieval and a policy check run in parallel. Retrieval at 80 milliseconds is on the path, the check at 30 is not.', check: { q: 'Two stages run in parallel at 80 and 30 milliseconds. Which sets latency?', options: ['Both added, 110', 'The 30 millisecond stage', 'The 80 millisecond stage'], answer: 2, explain: 'Parallel stages overlap. The slower one sets the time.' } },
    { id: 'd76-c13', term: 'Fan-out tail latency', plain: 'Fan-out calls many services at once. The slowest of them sets your tail latency.', why: 'If each call is fast 99 times in 100, calling ten of them is fast only about 90 times in 100. Tails multiply.', example: 'Eight retrievers in parallel. The request is as slow as the slowest of the eight.', check: { q: 'Calling more services in parallel does what to tail latency?', options: ['Nothing', 'Makes it worse', 'Makes it better'], answer: 1, explain: 'More calls means more chances that one is slow. Hedge or cap the fan-out.' } },
    { id: 'd76-c14', term: 'Failure domain', plain: 'A failure domain is everything that breaks together when one thing fails.', why: 'A shared database, region or vendor key puts many components in one domain. Small domains keep incidents small.', example: 'All services use one vendor key. Revoking that key stops every product at once.', check: { q: 'What shrinks a failure domain?', options: ['Separating shared dependencies', 'Adding more logging', 'A bigger model'], answer: 0, explain: 'Fewer shared parts means one failure takes down fewer components.' } },
    { id: 'd76-c15', term: 'Trust boundary', plain: 'A trust boundary is a line where data crosses from one level of trust to another.', why: 'Retrieved documents, user input and tool results are untrusted. The architecture must mark where they enter so the security review can start there.', example: 'The line between the retriever and the orchestrator. Passages cross it unverified and may carry injected instructions.', check: { q: 'Which crossing is a trust boundary?', options: ['Orchestrator calling its own cache', 'Gateway writing a log line', 'Retrieved web pages entering the prompt'], answer: 2, explain: 'Retrieved text comes from outside. Where it enters is where injection defence must sit.' } },
  ],
  equations: [
    { id: 'd76-e01', name: 'Latency sum', symbol: 'T = t₁ + t₂ + t₃', tokens: [{ sym: 'T', word: 'total latency' }, { sym: '=', word: 'is' }, { sym: 't₁', word: 'stage one time' }, { sym: '+', word: 'plus' }, { sym: 't₂', word: 'stage two time' }, { sym: 't₃', word: 'stage three time' }], literal: 'total latency is stage one time plus stage two time plus stage three time', meaning: 'On the critical path, stage times add. The budget is the sum, so any stage that grows pushes the total over.', example: 'Gateway 20, retrieval 80, model 600. Total 700 milliseconds.', check: { q: 'Stages of 50, 100 and 400 milliseconds in series. Total?', options: ['400', '550', '450'], answer: 1, explain: 'In series, times add. 50 plus 100 plus 400 is 550.' } },
    { id: 'd76-e02', name: 'Cache latency', symbol: 'T = h·tc + (1−h)·tm', tokens: [{ sym: 'T', word: 'average latency' }, { sym: '=', word: 'is' }, { sym: 'h·tc', word: 'hit-rate times cache-time' }, { sym: '+', word: 'plus' }, { sym: '(1−h)·tm', word: 'miss-rate times model-time' }], literal: 'average latency is hit-rate times cache-time plus miss-rate times model-time', meaning: 'The expected latency mixes fast hits and slow misses by their share. The same shape gives expected cost.', example: 'Hit rate 0.4, cache 10 milliseconds, model 600. Average is 4 plus 360, which is 364.', check: { q: 'Hit rate 0.5, cache 10, model 800 milliseconds. Average?', options: ['405', '810', '400'], answer: 0, explain: '0.5 times 10 is 5. 0.5 times 800 is 400. The sum is 405.' } },
    { id: 'd76-e03', name: 'Law of Little', symbol: 'L = λ·W', tokens: [{ sym: 'L', word: 'requests in flight' }, { sym: '=', word: 'is' }, { sym: 'λ', word: 'arrival rate' }, { sym: '·', word: 'times' }, { sym: 'W', word: 'time in system' }], literal: 'requests in flight is arrival rate times time in system', meaning: 'Concurrency equals arrivals per second times the seconds each one spends inside. It sizes worker pools and connection limits.', example: '50 requests per second, 2 seconds each. 100 requests in flight at any moment.', check: { q: '20 requests per second, each taking 3 seconds. In flight?', options: ['23', '6.7', '60'], answer: 2, explain: '20 times 3 is 60 requests inside the system at once.' } },
    { id: 'd76-e04', name: 'Fan-out speed', symbol: 'P = pⁿ', tokens: [{ sym: 'P', word: 'chance all fast' }, { sym: '=', word: 'is' }, { sym: 'p', word: 'chance one fast' }, { sym: 'ⁿ', word: 'to the power call-count' }], literal: 'chance all fast is chance one fast to the power call-count', meaning: 'With fan-out, every call must be fast for the request to be fast. Chances multiply, so tails grow with the number of parallel calls.', example: 'Each call fast 99 percent of the time. Ten calls. All fast about 90 percent of the time.', check: { q: 'Each of 5 parallel calls is fast with chance 0.9. All fast?', options: ['0.9', '0.59', '0.45'], answer: 1, explain: '0.9 to the power 5 is about 0.59.' } },
    { id: 'd76-e05', name: 'Chain uptime', symbol: 'A = A₁·A₂·A₃', tokens: [{ sym: 'A', word: 'system uptime' }, { sym: '=', word: 'is' }, { sym: 'A₁', word: 'stage one uptime' }, { sym: '·', word: 'times' }, { sym: 'A₂', word: 'stage two uptime' }, { sym: 'A₃', word: 'stage three uptime' }], literal: 'system uptime is stage one uptime times stage two uptime times stage three uptime', meaning: 'A chain is only up when every link is up. Uptimes multiply, so a chain of good parts is worse than any one of them.', example: 'Three stages at 0.99 each. System uptime 0.97, about 11 days down per year.', check: { q: 'Two stages each at 0.95 uptime in series. System uptime?', options: ['0.9025', '0.95', '0.975'], answer: 0, explain: '0.95 times 0.95 is 0.9025.' } },
  ],
  problems: [
    { id: 'd76-p01', prompt: 'Your budget is 800 milliseconds. Gateway 20, retrieval 80, model 600, formatting 50, all in series. Total and headroom?', hint: 'Add the stages, then subtract from the budget.', options: ['750 ms, 50 ms spare', '700 ms, 100 ms spare', '830 ms, over budget'], answer: 0, worked: '20 plus 80 plus 600 plus 50 is 750. The budget is 800, so 50 milliseconds remain for growth or retries.' },
    { id: 'd76-p02', prompt: 'Peak traffic is 50 requests per second and each request spends 2 seconds inside the system. How many workers must be busy at once?', hint: 'Arrival rate times time in the system.', options: ['25', '100', '52'], answer: 1, worked: '50 times 2 is 100. The pool needs at least 100 concurrent workers, plus headroom for bursts.' },
  ],
  build: { id: 'd76-b01', title: 'Latency budget sheet', goal: 'Write a script that sums stage latencies, compares them to a budget and names the biggest stage.', steps: ['Create a CSV of stages with columns name, p50 and p95 in milliseconds.', 'Read it with the csv module and sum p50 and p95 separately.', 'Compare each sum to a budget passed on the command line.', 'Print over or under, and the stage with the largest p95 share.'], done: 'The script prints the totals, the verdict and the dominant stage for any CSV.', check: { q: 'Why sum p95 separately from p50?', options: ['p95 is easier to measure', 'Slow requests are what users complain about', 'p50 is always wrong'], answer: 1, explain: 'The median hides the tail. The budget must hold for the slow requests too.' } },
  incident: { id: 'd76-i01', title: 'Model requests arrive without gateway ids', signal: 'The model service log shows three percent of requests carry no gateway request id. The gateway log has no matching entries for them.', clue: 'The model service listens on a port reachable from the whole internal network.', options: ['An internal client bypasses the gateway, restrict the port to it', 'The gateway is dropping ids, restart it', 'A logging bug, ignore it'], answer: 0, explain: 'Requests with no gateway trace skipped authentication and rate limits. Find the client from source addresses, then allow only the gateway to reach the model port.' },
  research: { id: 'd76-r01', question: 'Does a semantic cache cut model cost more than it harms answer freshness on support traffic?', hypothesis: 'A cache at similarity 0.95 removes at least 30 percent of model calls with under one percent stale answers.', method: 'Replay one week of traffic through the cache offline. Count hits, and have reviewers label a sample of hits as fresh or stale.', check: { q: 'Why replay real traffic instead of synthetic queries?', options: ['It is cheaper', 'Real repetition rates decide the hit rate', 'Synthetic queries are not allowed'], answer: 1, explain: 'Hit rate depends on how often real users repeat themselves. Synthetic data would invent that number.' } },
  decision: { id: 'd76-l01', situation: 'A four person team proposes nine separate services for a single support assistant, each with its own deployment. The product launches in ten weeks.', options: ['Approve the nine services', 'One service behind a gateway with clear internal interfaces', 'Buy a full platform instead'], answer: 1, reasoning: 'Nine deployments for four people is operational load, not architecture. Clear interfaces inside one service keep the option to split later, when traffic or team size justifies it.' },
};

export default day;
