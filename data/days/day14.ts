import type { Day } from '../types';

const day: Day = {
  day: 14,
  title: 'Networks',
  goal: 'Follow a request from a phone to a server and back.',
  concepts: [
    { id: 'd14-c01', term: 'Network', plain: 'A network is a set of computers joined so they can send each other messages.', why: 'Every model you serve, every dataset you fetch and every attack you defend against travels over a network.', example: 'Your phone, a wifi router and a web server form a network when you load a page.', check: { q: 'What is a network?', options: ['Computers joined so they can exchange messages', 'A single fast computer', 'A type of database'], answer: 0, explain: 'A network is about links between machines, not about any one machine.' } },
    { id: 'd14-c02', term: 'Packet', plain: 'A packet is a small chunk of data with an address on the front.', why: 'Large files do not travel whole. They are cut into packets that can take different routes and arrive out of order.', example: 'A 3 megabyte photo becomes about 2000 packets of 1500 bytes each.', check: { q: 'Why is data sent as packets rather than one whole message?', options: ['Packets are encrypted by default', 'Small pieces can share links and be resent alone', 'Servers cannot store whole files'], answer: 1, explain: 'Packets let many senders share a link, and a lost piece is resent without resending everything.' } },
    { id: 'd14-c03', term: 'IP address', plain: 'An IP address is a number that names one machine on a network.', why: 'Every packet carries a source and a destination address. Without them a router cannot know where to send it.', example: '192.168.1.10 is a typical home address. 8.8.8.8 is a public Google server.', check: { q: 'What does an IP address name?', options: ['A program', 'A cable', 'A machine on the network'], answer: 2, explain: 'The IP address identifies a machine. The port, coming next, identifies a program on it.' } },
    { id: 'd14-c04', term: 'Port', plain: 'A port is a number that names one program on a machine.', why: 'One server runs many services. Ports let a packet reach the web server and not the mail server.', example: 'Web traffic uses port 443 for HTTPS and port 80 for plain HTTP.', check: { q: 'A packet arrives at a server for port 443. Which program receives it?', options: ['The email service', 'The web service', 'The first program started'], answer: 1, explain: 'Port 443 is the standard port for HTTPS, so the web server receives it.' } },
    { id: 'd14-c05', term: 'Router', plain: 'A router is a device that passes packets one step closer to their destination.', why: 'No machine knows the whole internet. Each router only knows the next hop, and the chain of hops delivers the packet.', example: 'A packet from London to Tokyo may pass through fifteen routers, each choosing the next link.', check: { q: 'What does a router decide for each packet?', options: ['The next hop towards the destination', 'The full path to the destination', 'Whether the content is safe'], answer: 0, explain: 'A router reads the destination address and forwards the packet to the next router only.' } },
    { id: 'd14-c06', term: 'Protocol', plain: 'A protocol is an agreed set of rules for how two machines talk.', why: 'Machines from different makers can only communicate because they follow the same written rules for format and order.', example: 'HTTP says a request starts with a verb like GET, then a path, then headers.', check: { q: 'What is a protocol?', options: ['A brand of router', 'A fast cable', 'Agreed rules for exchanging messages'], answer: 2, explain: 'A protocol is a shared rulebook. Both sides follow it so each understands the other.' } },
    { id: 'd14-c07', term: 'Layers', plain: 'Networking is built in layers, each one using the layer below and hiding it.', why: 'Layers let you swap wifi for cable without changing the web app. Each layer solves one job.', example: 'Cable or wifi at the bottom, then IP addresses, then TCP, then HTTP at the top.', check: { q: 'Why are networks built in layers?', options: ['Each layer does one job and can swap', 'To make packets larger', 'Because routers need more work'], answer: 0, explain: 'Each layer does one job and only talks to its neighbours, so parts can change independently.' } },
    { id: 'd14-c08', term: 'DNS', plain: 'DNS turns a name people can read into an IP address machines can use.', why: 'Nobody remembers numbers. DNS is the phone book, and when it fails whole sites seem to vanish.', example: 'Looking up example.com returns 93.184.216.34 before any page is fetched.', check: { q: 'What does DNS do?', options: ['Encrypts web pages', 'Speeds up downloads', 'Maps names to addresses'], answer: 2, explain: 'DNS is a lookup service. Name in, IP address out.' } },
    { id: 'd14-c09', term: 'TCP', plain: 'TCP delivers packets reliably, in order, and resends any that go missing.', why: 'Web pages, files and model weights must arrive complete and in order. TCP does that bookkeeping for you.', example: 'If packet 7 of 100 is lost, the receiver asks again and TCP resends it.', check: { q: 'What does TCP guarantee?', options: ['Complete and ordered delivery', 'The fastest possible speed', 'That the sender is trusted'], answer: 0, explain: 'TCP numbers packets, acknowledges them and resends losses. It trades speed for reliability.' } },
    { id: 'd14-c10', term: 'UDP', plain: 'UDP sends packets fast with no promise that they arrive or arrive in order.', why: 'Voice, video and games prefer a dropped frame over a late one. UDP skips the waiting.', example: 'A video call over UDP drops a few frames rather than freezing to resend them.', check: { q: 'Which use suits UDP better than TCP?', options: ['Downloading model weights', 'Sending a bank transfer', 'A live voice call'], answer: 2, explain: 'A voice call would rather lose a moment than pause for a resend.' } },
    { id: 'd14-c11', term: 'HTTP', plain: 'HTTP is the request and response protocol behind the web and most APIs.', why: 'Every model API call is an HTTP request. Reading its status code tells you what went wrong.', example: 'GET /models returns 200 for success, 404 for not found, 500 for a server error.', check: { q: 'An API returns status 500. What does it mean?', options: ['The request was malformed', 'The server hit an error', 'The page was not found'], answer: 1, explain: 'Codes starting with 5 mean the server failed. Codes starting with 4 blame the request.' } },
    { id: 'd14-c12', term: 'Latency', plain: 'Latency is the time a message takes to travel there and back.', why: 'Light takes about 130 milliseconds to cross the world and return. No code makes that faster.', example: 'London to New York round trip is about 70 milliseconds on a good link.', check: { q: 'What mainly sets the lowest possible latency between two cities?', options: ['Distance and the speed of light', 'Server memory size', 'The number of users'], answer: 0, explain: 'Signals cannot beat the speed of light. Distance sets a floor no software can remove.' } },
    { id: 'd14-c13', term: 'Bandwidth', plain: 'Bandwidth is how much data a link can carry each second.', why: 'Latency is how quickly the first byte arrives. Bandwidth is how quickly the rest follow.', example: 'A 100 megabit link moves about 12 megabytes per second at best.', check: { q: 'A link has high bandwidth but high latency. What is it good for?', options: ['Large file transfers', 'Fast turn-based games', 'Voice calls'], answer: 0, explain: 'Once the first byte arrives, high bandwidth moves the bulk quickly. Latency hurts only quick exchanges.' } },
    { id: 'd14-c14', term: 'TLS', plain: 'TLS encrypts a connection so nobody in the middle can read or alter it.', why: 'Prompts, API keys and personal data cross many routers you do not own. TLS keeps them private.', example: 'HTTPS is HTTP inside TLS. The padlock in the browser means TLS is on.', check: { q: 'What does TLS protect against?', options: ['Slow servers', 'Eavesdropping in transit', 'Running out of port numbers'], answer: 1, explain: 'TLS encrypts and checks the data between the two ends, so middle boxes cannot read or tamper.' } },
    { id: 'd14-c15', term: 'Congestion control', plain: 'Congestion control makes TCP slow down when packets are lost, so links do not collapse.', why: 'A busy link drops packets. Every sender backing off keeps the network usable for everyone.', example: 'TCP grows its sending rate until a loss, then halves it and climbs again.', check: { q: 'What does TCP do when it detects packet loss?', options: ['Sends faster to catch up', 'Switches to UDP', 'Cuts its sending rate'], answer: 2, explain: 'Loss usually means a full link. Slowing down clears the queue and shares capacity fairly.' } },
  ],
  equations: [
    { id: 'd14-e01', name: 'Transfer time', symbol: 't = S / B', tokens: [{ sym: 't', word: 'time' }, { sym: '=', word: 'is' }, { sym: 'S', word: 'size' }, { sym: '/', word: 'divided by' }, { sym: 'B', word: 'bandwidth' }], literal: 'time is size divided by bandwidth', meaning: 'How long the bulk of a transfer takes once the link is busy. Bigger files or thinner links take longer.', example: 'A 40 megabyte file over an 8 megabyte per second link takes 5 seconds.', check: { q: 'A 60 megabyte file over 20 megabytes per second. How long?', options: ['3 seconds', '1200 seconds', '0.3 seconds'], answer: 0, explain: '60 divided by 20 is 3. Size divided by bandwidth gives time.' } },
    { id: 'd14-e02', name: 'Round trip', symbol: 'RTT = 2 × d', tokens: [{ sym: 'RTT', word: 'round-trip' }, { sym: '=', word: 'is' }, { sym: '2', word: 'two' }, { sym: '×', word: 'times' }, { sym: 'd', word: 'one-way delay' }], literal: 'round-trip is two times one-way delay', meaning: 'A reply cannot come back faster than twice the time to get there. Ping measures the round trip.', example: 'One-way delay of 35 milliseconds gives a round trip of 70 milliseconds.', check: { q: 'One-way delay is 50 milliseconds. What is the round trip?', options: ['25 milliseconds', '100 milliseconds', '50 milliseconds'], answer: 1, explain: 'Double the one-way delay. Two times 50 is 100 milliseconds.' } },
    { id: 'd14-e03', name: 'Bandwidth delay product', symbol: 'C = B × RTT', tokens: [{ sym: 'C', word: 'data in flight' }, { sym: '=', word: 'is' }, { sym: 'B', word: 'bandwidth' }, { sym: '×', word: 'times' }, { sym: 'RTT', word: 'round-trip' }], literal: 'data in flight is bandwidth times round-trip', meaning: 'How much data must be in the air to keep a link full. Small buffers on a long fat link waste bandwidth.', example: '10 megabytes per second times 0.1 seconds round trip means 1 megabyte in flight.', check: { q: 'Bandwidth 5 megabytes per second, round trip 0.2 seconds. Data in flight?', options: ['25 megabytes', '0.04 megabytes', '1 megabyte'], answer: 2, explain: '5 times 0.2 is 1. One megabyte must be in flight to fill the link.' } },
    { id: 'd14-e04', name: 'Usable hosts', symbol: 'N = 2⁸ − 2', tokens: [{ sym: 'N', word: 'usable hosts' }, { sym: '=', word: 'is' }, { sym: '2⁸', word: 'two to the eighth' }, { sym: '−', word: 'minus' }, { sym: '2', word: 'two' }], literal: 'usable hosts is two to the eighth minus two', meaning: 'A slash 24 network has 8 host bits, so 256 addresses. Two are reserved, leaving 254 for machines.', example: '192.168.1.0 slash 24 gives addresses 1 to 254 for devices, so 254 hosts.', check: { q: 'Why subtract two from the address count?', options: ['Two addresses are always faulty', 'The network and broadcast addresses are reserved', 'Routers need two each'], answer: 1, explain: 'The first address names the network and the last is broadcast. Neither goes to a host.' } },
    { id: 'd14-e05', name: 'Total fetch time', symbol: 'T = RTT + S / B', tokens: [{ sym: 'T', word: 'total time' }, { sym: '=', word: 'is' }, { sym: 'RTT', word: 'round-trip' }, { sym: '+', word: 'plus' }, { sym: 'S', word: 'size' }, { sym: '/', word: 'divided by' }, { sym: 'B', word: 'bandwidth' }], literal: 'total time is round-trip plus size divided by bandwidth', meaning: 'A fetch pays the round trip first, then the transfer. For small responses the round trip dominates.', example: 'Round trip 0.1 seconds plus 2 megabytes at 10 megabytes per second gives 0.3 seconds.', check: { q: 'Round trip 0.2 seconds, 1 megabyte at 10 megabytes per second. Total?', options: ['0.3 seconds', '2.2 seconds', '0.12 seconds'], answer: 0, explain: '0.2 plus 1 divided by 10 is 0.2 plus 0.1, which is 0.3 seconds.' } },
  ],
  problems: [
    { id: 'd14-p01', prompt: 'A 50 megabyte model checkpoint downloads over a 10 megabyte per second link. How long does the transfer take?', hint: 'Divide the size by the bandwidth.', options: ['5 seconds', '500 seconds', '0.2 seconds'], answer: 0, worked: '50 divided by 10 is 5. The transfer takes about 5 seconds, plus one round trip to start.' },
    { id: 'd14-p02', prompt: 'A link carries 100 megabits per second and the round trip is 0.2 seconds. How much data must be in flight to fill it?', hint: 'Multiply bandwidth by round-trip time.', options: ['500 megabits', '20 megabits', '0.002 megabits'], answer: 1, worked: '100 times 0.2 is 20. Twenty megabits must be in flight or the link sits partly idle.' },
  ],
  build: { id: 'd14-b01', title: 'Latency probe', goal: 'Write a Python script that measures connection time to three servers and reports the average.', steps: ['Import socket and time, and list three hostnames.', 'For each host, time a socket connect to port 443.', 'Repeat five times per host and store the milliseconds.', 'Print each host with its average and slowest time.'], done: 'The script prints average connect time in milliseconds for three hosts.', check: { q: 'Why measure five times rather than once?', options: ['Single measurements are noisy', 'It warms up the CPU', 'DNS needs five tries'], answer: 0, explain: 'One reading can hit a busy moment. Several readings show the typical value and the spread.' } },
  incident: { id: 'd14-i01', title: 'Website slow for one office only', signal: 'Staff in the Manchester office see pages load in 8 seconds. Every other office sees 200 milliseconds. Server load is normal.', clue: 'A traceroute from Manchester shows 40 percent packet loss at the second hop.', options: ['A faulty link at the office edge router', 'The web server is overloaded', 'The DNS server is down'], answer: 0, explain: 'Loss at the second hop is the office uplink. TCP resends and backs off, so pages crawl. Check the router and cable, then fail over to the spare link.' },
  research: { id: 'd14-r01', question: 'Does placing an inference server nearer users cut response time more than a faster model does?', hypothesis: 'For responses under 100 milliseconds of compute, network round trip dominates and location matters most.', method: 'Deploy the same model in two regions. From one city, time 1000 requests to each and compare against the compute time alone.', check: { q: 'What must stay the same between the two regions?', options: ['The model and the requests', 'The distance to the user', 'Only the time of day'], answer: 0, explain: 'Only location should differ. Same model and same requests make the comparison fair.' } },
  decision: { id: 'd14-l01', situation: 'Your assistant streams tokens to users in Sydney from a London server. Users complain the first word takes two seconds. Engineers propose a smaller model to speed it up.', options: ['Switch to a smaller model', 'Deploy the same model in an Australian region', 'Add more London servers'], answer: 1, reasoning: 'Sydney to London round trip is about 300 milliseconds before any compute. Distance, not model size, dominates first-token delay. A local region removes it. A smaller model cuts quality for little gain.' },
};

export default day;
