import type { Day } from '../types';

const day: Day = {
  day: 26,
  title: 'Transformers',
  goal: 'Explain how a transformer turns a sequence of tokens into predictions, layer by layer.',
  concepts: [
    { id: 'd26-c01', term: 'Sequence model', plain: 'A sequence model reads items in order and produces an output that depends on all of them.', why: 'Text, audio and code are sequences. The meaning of one token depends on its neighbours, so the model must see them together.', example: 'The word bank means one thing after river and another after savings.', check: { q: 'What makes a sequence model different from a plain classifier?', options: ['It reads items in order together', 'It is always bigger', 'It only handles numbers'], answer: 0, explain: 'A sequence model uses order and context. A plain classifier treats each input alone.' } },
    { id: 'd26-c02', term: 'Transformer', plain: 'A transformer is a stack of identical layers that mix tokens with attention and process each with a small network.', why: 'Every position is processed in parallel, so training runs fast on GPUs. Nearly every modern language model is a transformer.', example: 'GPT style models are decoder transformers. BERT is an encoder transformer.', check: { q: 'What are the two main parts of every transformer layer?', options: ['Convolution and pooling', 'Attention and a feed-forward network', 'A recurrent cell and a gate'], answer: 1, explain: 'Attention mixes information across positions. The feed-forward network transforms each position on its own.' } },
    { id: 'd26-c03', term: 'Positional encoding', plain: 'Attention has no idea of order, so a position signal is added to each token embedding.', why: 'Without positions, the cat sat and sat the cat look identical to the model. Order carries meaning.', example: 'Token three gets its embedding plus a vector that says third. Token four gets a different one.', check: { q: 'Why does a transformer need positional encoding?', options: ['To make training faster', 'To shrink the vocabulary', 'Because attention alone ignores order'], answer: 2, explain: 'Attention treats tokens as a set. Position vectors put the order back in.' } },
    { id: 'd26-c04', term: 'Residual connection', plain: 'A residual connection adds the input of a block to its output.', why: 'It gives gradients a direct path through many layers, so deep stacks train instead of stalling.', example: 'Output is input plus attention of input. If attention learns nothing, the token passes through unchanged.', check: { q: 'What does a residual connection do?', options: ['Adds block input to block output', 'Removes unused tokens', 'Doubles the learning-speed'], answer: 0, explain: 'The input skips around the block and is added back. Gradients flow through the skip.' } },
    { id: 'd26-c05', term: 'Layer normalisation', plain: 'Layer normalisation rescales each token vector to average zero and spread one before a block.', why: 'Vectors drift in size as they pass through layers. Keeping them steady keeps gradients sane and training stable.', example: 'A vector with values 10, 20, 30 becomes roughly minus 1.2, 0, 1.2.', check: { q: 'What does layer normalisation change about a token vector?', options: ['Its position in the sequence', 'Its size and centre', 'Its meaning'], answer: 1, explain: 'It shifts to average zero and scales to spread one. The direction of the vector stays.' } },
    { id: 'd26-c06', term: 'Feed-forward block', plain: 'The feed-forward block applies the same two-layer network to each token on its own.', why: 'Attention gathers information. The feed-forward block does the thinking on what was gathered, and holds most of the parameters.', example: 'Width 768 expands to 3,072, passes a rectifier, then shrinks back to 768.', check: { q: 'How many tokens does the feed-forward block look at when processing one token?', options: ['All of them', 'Only the previous one', 'Just that one'], answer: 2, explain: 'It works position by position. Mixing across tokens is the job of attention.' } },
    { id: 'd26-c07', term: 'Multi-head attention', plain: 'Multi-head attention runs several attention functions side by side and joins their outputs.', why: 'One head can track grammar while another tracks names. Several small heads see more than one big one.', example: 'Width 768 with 12 heads gives each head 64 dimensions. Outputs are joined back to 768.', check: { q: 'Why use several attention heads instead of one?', options: ['Each head can learn a different relation', 'It uses fewer parameters', 'Heads are required by GPUs'], answer: 0, explain: 'Different heads attend to different things. Together they capture more structure than one.' } },
    { id: 'd26-c08', term: 'Transformer block', plain: 'A block is attention, add and normalise, feed-forward, add and normalise.', why: 'This four-step recipe is the whole trick. Everything else is repeating it and choosing sizes.', example: 'Token vectors enter, attention mixes them, residual adds, feed-forward refines, residual adds, next block.', check: { q: 'In what order does a block apply its parts?', options: ['Feed-forward, then attention, no residuals', 'Attention with residual, then feed-forward with residual', 'Normalise once at the end only'], answer: 1, explain: 'Attention first, add the input, then feed-forward, add again. Normalisation sits around each.' } },
    { id: 'd26-c09', term: 'Stacking depth', plain: 'Stacking many blocks lets later layers reason about patterns that earlier layers found.', why: 'Early layers handle spelling and syntax. Middle layers handle meaning. Late layers pick the next token.', example: 'GPT-2 small has 12 blocks. Large models have 80 or more.', check: { q: 'What do deeper layers tend to do?', options: ['Repeat what layer one did', 'Build higher-level patterns from earlier ones', 'Only add positions'], answer: 1, explain: 'Each layer refines the representation. Depth lets simple features combine into complex ones.' } },
    { id: 'd26-c10', term: 'Encoder', plain: 'An encoder reads the whole input at once and every token can attend to every other.', why: 'For understanding tasks like classification or search, seeing both sides of a word gives the best representation.', example: 'BERT reads a sentence both ways and outputs one vector per token for classification.', check: { q: 'Which direction can encoder attention look?', options: ['Both directions', 'Only backwards', 'Only forwards'], answer: 0, explain: 'Encoders are bidirectional. Each token sees the full input.' } },
    { id: 'd26-c11', term: 'Decoder', plain: 'A decoder generates output one token at a time, seeing only what came before.', why: 'Generation must not peek at the future, because at run time the future does not exist yet.', example: 'Given The cat sat on the, the decoder scores every possible next token and picks mat.', check: { q: 'What can a decoder token attend to?', options: ['Tokens after it', 'Itself and earlier tokens', 'Nothing, it works alone'], answer: 1, explain: 'Decoders look backwards only. This matches how text is produced at run time.' } },
    { id: 'd26-c12', term: 'Causal mask', plain: 'A causal mask sets attention scores to the future to minus infinity so they get zero weight.', why: 'It lets one training pass predict every next token at once, without any position cheating.', example: 'Token 3 sees tokens 1, 2 and 3. Scores for tokens 4 and 5 are masked out.', check: { q: 'What does the causal mask hide?', options: ['Padding tokens only', 'Rare words', 'Future positions'], answer: 2, explain: 'Future positions are masked. Each token predicts the next using only the past.' } },
    { id: 'd26-c13', term: 'Output head', plain: 'The output head turns the final vector into a score for every token in the vocabulary.', why: 'A single linear layer plus softmax gives the probability of each next token. This is what training compares to the truth.', example: 'Width 768 maps to 50,257 scores. Softmax turns them into probabilities that add up to one.', check: { q: 'What does the output head produce?', options: ['One probability per vocabulary token', 'A single number', 'A new embedding table'], answer: 0, explain: 'It scores every vocabulary entry. Softmax makes the scores probabilities.' } },
    { id: 'd26-c14', term: 'Parameter count', plain: 'Most parameters live in the attention and feed-forward weights and grow with width squared.', why: 'Doubling width quadruples parameters. Knowing where they live tells you memory, cost and speed before you train.', example: 'Twelve layers at width 768 hold about 85 million parameters plus a 38 million embedding table.', check: { q: 'What happens to parameter count when you double the width?', options: ['It doubles', 'It stays the same', 'It roughly quadruples'], answer: 2, explain: 'Weight matrices are width by width. Double the width and each matrix is four times bigger.' } },
    { id: 'd26-c15', term: 'KV cache', plain: 'A KV cache stores keys and values of past tokens so each new token only computes its own.', why: 'Without it, generating token 1,000 recomputes 999 tokens. With it, cost per token stays flat, but memory grows.', example: 'A 4,000 token chat keeps 4,000 key and value vectors per layer per head in GPU memory.', check: { q: 'What does the KV cache trade?', options: ['Accuracy for speed', 'Memory for speed', 'Speed for memory'], answer: 1, explain: 'It spends GPU memory on stored keys and values to avoid recomputing them each step.' } },
  ],
  equations: [
    { id: 'd26-e01', name: 'Position added', symbol: 'h = e + p', tokens: [{ sym: 'h', word: 'hidden' }, { sym: '=', word: 'is' }, { sym: 'e', word: 'embedding' }, { sym: '+', word: 'plus' }, { sym: 'p', word: 'position code' }], literal: 'hidden is embedding plus position code', meaning: 'The starting vector for each token is its meaning vector plus a vector that says where it sits.', example: 'Embedding 0.2, 0.5 plus position 0.1, minus 0.1 gives hidden 0.3, 0.4.', check: { q: 'Why add rather than replace the embedding?', options: ['Both meaning and position are kept', 'Adding is faster', 'Replacing is not allowed'], answer: 0, explain: 'The sum carries both signals. Attention can then use either.' } },
    { id: 'd26-e02', name: 'Residual update', symbol: 'h′ = h + f(h)', tokens: [{ sym: 'h′', word: 'hidden next' }, { sym: '=', word: 'is' }, { sym: 'h', word: 'hidden' }, { sym: '+', word: 'plus' }, { sym: 'f(h)', word: 'block applied to hidden' }], literal: 'hidden next is hidden plus block applied to hidden', meaning: 'Each block only adds a correction to what came in. The original signal always survives.', example: 'Hidden 1.0 and block output 0.3 give hidden next 1.3.', check: { q: 'If the block outputs all zeros, what is hidden next?', options: ['Zero', 'The same as hidden', 'Undefined'], answer: 1, explain: 'Hidden plus zero is hidden. The skip path passes it straight through.' } },
    { id: 'd26-e03', name: 'Layer norm', symbol: 'ĥ = (h − μ) / √σ²', tokens: [{ sym: 'ĥ', word: 'normalised hidden' }, { sym: '=', word: 'is' }, { sym: 'h', word: 'hidden' }, { sym: '−', word: 'minus' }, { sym: 'μ', word: 'average' }, { sym: '/', word: 'divided by' }, { sym: '√σ²', word: 'square root of spread' }], literal: 'normalised hidden is hidden minus average divided by square root of spread', meaning: 'Subtract the average of the vector, then divide by the square root of its spread. Every token ends the same size.', example: 'Values 10, 20, 30 have average 20 and spread 66.7, giving minus 1.2, 0, 1.2.', check: { q: 'What is the average of the normalised vector?', options: ['One', 'The original average', 'Zero'], answer: 2, explain: 'Subtracting the average centres the values on zero.' } },
    { id: 'd26-e04', name: 'Feed-forward block', symbol: 'FFN(h) = W₂ · relu(W₁ · h)', tokens: [{ sym: 'FFN(h)', word: 'feed-forward of hidden' }, { sym: '=', word: 'is' }, { sym: 'W₂', word: 'second weight' }, { sym: '·', word: 'times' }, { sym: 'relu', word: 'rectify' }, { sym: 'W₁', word: 'first weight' }, { sym: 'h', word: 'hidden' }], literal: 'feed-forward of hidden is second weight times rectify first weight times hidden', meaning: 'Expand the vector with the first weight, cut negatives to zero, then shrink it back with the second weight.', example: 'First weight maps 768 to 3,072. Rectify. Second weight maps 3,072 back to 768.', check: { q: 'What does rectify do to the expanded vector?', options: ['Sets negative values to zero', 'Sorts it', 'Normalises it'], answer: 0, explain: 'Rectify keeps positives and zeroes negatives. It is the nonlinearity.' } },
    { id: 'd26-e05', name: 'Parameter count', symbol: 'N ≈ 12 · n · d²', tokens: [{ sym: 'N', word: 'parameter count' }, { sym: '≈', word: 'is approximately' }, { sym: '12', word: 'twelve' }, { sym: '·', word: 'times' }, { sym: 'n', word: 'layer count' }, { sym: 'd²', word: 'width squared' }], literal: 'parameter count is approximately twelve times layer count times width squared', meaning: 'Each block holds about twelve width-squared weights, four in attention and eight in feed-forward. Multiply by the number of blocks.', example: 'Twelve layers at width 768 give 12 times 12 times 589,824, about 85 million.', check: { q: 'Which term does the count depend on most strongly?', options: ['Layer count', 'Width, because it is squared', 'The constant twelve'], answer: 1, explain: 'Width is squared. Doubling it quadruples the count, doubling layers only doubles it.' } },
  ],
  problems: [
    { id: 'd26-p01', prompt: 'A transformer has width 1,024 and 24 layers. Approximately how many parameters sit in its blocks, ignoring the embedding table?', hint: 'Twelve times layers times width squared.', options: ['About 302 million', 'About 25 million', 'About 3 billion'], answer: 0, worked: '1,024 squared is 1,048,576. Times 24 layers times 12 is about 302 million.' },
    { id: 'd26-p02', prompt: 'A decoder generates 100 tokens with no KV cache. How many token positions get their keys and values computed in total?', hint: 'Step t recomputes t positions. Add them up.', options: ['100', '5,050', '10,000'], answer: 1, worked: 'Step 1 computes 1, step 2 computes 2, up to 100. The sum 1 to 100 is 5,050. A cache needs only 100.' },
  ],
  build: { id: 'd26-b01', title: 'One transformer block', goal: 'Build a single transformer block in PyTorch and confirm it keeps shape and passes gradients.', steps: ['Create nn.LayerNorm, nn.MultiheadAttention and a two-layer feed-forward in PyTorch.', 'Write forward as x plus attention of normalised x.', 'Then add x plus feed-forward of normalised x.', 'Feed a random batch of shape 2 by 10 by 64 and print shapes.'], done: 'Output shape matches input shape and every parameter has a gradient after one backward pass.', check: { q: 'Why add the input back after each sub-block?', options: ['To keep gradients flowing through deep stacks', 'To save memory', 'To make outputs smaller'], answer: 0, explain: 'The residual path lets gradients skip the block. Deep stacks train because of it.' } },
  incident: { id: 'd26-i01', title: 'Chat turns to nonsense past 512 tokens', signal: 'A chat service returns garbage once a conversation passes 512 tokens. Shorter chats are fine. The model file is unchanged.', clue: 'The model was trained with learned position embeddings of length 512.', options: ['Positions beyond 512 have no learned embedding', 'The GPU is overheating', 'The tokeniser was swapped'], answer: 0, explain: 'The learned position table ends at 512, so later positions read undefined vectors. Truncate context to 512 or move to a model with rotary positions.' },
  research: { id: 'd26-r01', question: 'Does removing layer normalisation from a small transformer slow or stop convergence?', hypothesis: 'Without normalisation, a six-layer model diverges within a thousand steps at the same learning-speed.', method: 'Train two identical six-layer models on the same text, one with layer norm and one without. Log loss every hundred steps for ten thousand steps.', check: { q: 'What must stay the same between the two runs?', options: ['Everything except layer norm', 'Only the dataset', 'Only the random seed'], answer: 0, explain: 'One change at a time. If anything else differs, the result cannot be attributed to normalisation.' } },
  decision: { id: 'd26-l01', situation: 'A product needs to summarise 200-page contracts. Your current model handles 4,000 tokens. Engineers propose training a new model with a 200,000 token context from scratch.', options: ['Train a new long-context model from scratch', 'Chunk documents and use an existing long-context model', 'Reject the feature'], answer: 1, reasoning: 'Training from scratch costs months and millions with no guarantee. Chunking with an existing long-context model ships in weeks and can be measured against real contracts before any bigger bet.' },
};

export default day;
