import type { Day } from '../types';

const day: Day = {
  day: 52,
  title: 'Model serving',
  goal: 'Serve a language model with measured latency, batching and a bounded memory budget.',
  concepts: [
    { id: 'd52-c01', term: 'Inference server', plain: 'An inference server loads a model once and answers many requests over HTTP.', why: 'Loading weights takes minutes. Keeping the model resident and sharing it across requests is what makes serving affordable.', example: 'vLLM loads Llama once, then serves a POST to /v1/completions in milliseconds.', check: { q: 'Why keep the model loaded between requests?', options: ['HTTP requires it', 'Models forget otherwise', 'Loading weights takes far longer than answering'], answer: 2, explain: 'Weights take minutes to load. Answering takes milliseconds. Load once, serve many.' } },
    { id: 'd52-c02', term: 'Prefill and decode', plain: 'Prefill processes the whole prompt in one pass. Decode then produces one token at a time.', why: 'Prefill is compute bound and fast per token. Decode is memory bound and slow per token. They need different optimisations.', example: 'A 2000 token prompt prefills in 200 milliseconds. Each output token then takes 25.', check: { q: 'Which phase is memory bound?', options: ['Prefill', 'Decode', 'Neither'], answer: 1, explain: 'Decode reads all the weights for every single token. That is a bandwidth limit.' } },
    { id: 'd52-c03', term: 'Time to first token', plain: 'Time to first token is the wait from sending a prompt to seeing the first output token.', why: 'Users judge speed by the first token, not the last. It is mostly prefill time plus any queueing.', example: 'Prompt sent at 0 milliseconds, first token at 350. Time to first token is 350.', check: { q: 'What mostly determines time to first token?', options: ['Prefill time plus queueing', 'Number of output tokens', 'Disk speed'], answer: 0, explain: 'The prompt must be prefilled before any token appears. Waiting in a queue adds to it.' } },
    { id: 'd52-c04', term: 'Latency percentiles', plain: 'The p95 latency is the time that 95 percent of requests beat. p50 is the median.', why: 'Averages hide the slow tail. One user in twenty sees the p95. Service targets are set on tails, not means.', example: 'p50 is 400 milliseconds and p99 is 4 seconds. One in a hundred waits ten times longer.', check: { q: 'Why report p95 rather than the mean?', options: ['The mean is hard to compute', 'The mean is always higher', 'The mean hides the slow tail'], answer: 2, explain: 'A few very slow requests barely move the mean but hurt real users. Percentiles show them.' } },
    { id: 'd52-c05', term: 'Throughput versus latency', plain: 'Bigger batches raise tokens per second for the server but add wait time for each request.', why: 'You cannot maximise both. Pick a latency target, then push throughput as high as it allows.', example: 'Batch 1 gives 40 tokens a second at 25 milliseconds each. Batch 32 gives 900 at 35.', check: { q: 'What happens to per-request latency as batch size grows?', options: ['It falls', 'It rises slowly', 'It stays exactly the same'], answer: 1, explain: 'Each step takes slightly longer with more sequences. Throughput climbs much faster than latency.' } },
    { id: 'd52-c06', term: 'Streaming responses', plain: 'Streaming sends each token to the client as it is produced instead of waiting for the end.', why: 'A 500 token answer takes over 10 seconds. Streaming makes the wait feel short and lets users stop early.', example: 'Server sent events deliver data lines, one per token, over a single HTTP response.', check: { q: 'What does streaming change for the user?', options: ['They see the first token much sooner', 'Total generation time drops', 'Answers become shorter'], answer: 0, explain: 'Generation takes the same time. The user sees progress from the first token.' } },
    { id: 'd52-c07', term: 'Static batching', plain: 'Static batching waits to collect several requests, runs them together, and returns when all finish.', why: 'It shares the weight read across requests, but short answers wait for the longest one in the batch.', example: 'Eight requests start together. Seven finish at 100 tokens. All wait for the one at 800.', check: { q: 'What is the weakness of static batching?', options: ['Short requests wait for the longest one', 'It uses more memory per request', 'It cannot run on GPUs'], answer: 0, explain: 'The batch returns only when every sequence is done. Finished slots sit idle.' } },
    { id: 'd52-c08', term: 'Continuous batching', plain: 'Continuous batching adds and removes requests from the batch at every decode step.', why: 'A finished sequence frees its slot at once and a waiting request takes it. GPU slots stay full and tail latency drops.', example: 'vLLM and TensorRT-LLM both schedule per step. Throughput often doubles over static batching.', check: { q: 'When does a new request join a continuous batch?', options: ['At the start of the next full batch', 'At the next decode step with a free slot', 'Only when the batch is empty'], answer: 1, explain: 'Scheduling happens every step. Free slots are refilled immediately.' } },
    { id: 'd52-c09', term: 'KV cache', plain: 'The KV cache stores the attention keys and values for every token seen so far.', why: 'Without it, each new token would recompute attention over the whole prompt. With it, memory grows with every token of every request.', example: 'A 7 billion model stores about half a megabyte per token. A 4000 token chat holds 2 gigabytes.', check: { q: 'What does the KV cache save?', options: ['Disk reads', 'Recomputing attention over past tokens', 'Weight downloads'], answer: 1, explain: 'Past keys and values are kept, so each new token only computes its own.' } },
    { id: 'd52-c10', term: 'Paged attention', plain: 'Paged attention stores the KV cache in small fixed blocks instead of one reserved strip per request.', why: 'Reserving the maximum length for every request wastes most of the memory. Blocks are allocated only as tokens arrive.', example: 'Blocks of 16 tokens. A 100 token request uses 7 blocks, not a 4096 token reservation.', check: { q: 'What problem does paged attention solve?', options: ['Slow prefill', 'Low precision weights', 'Wasted cache memory from over-reservation'], answer: 2, explain: 'Fixed reservations leave most cache memory empty. Paging fills it with real tokens.' } },
    { id: 'd52-c11', term: 'Prefix caching', plain: 'Prefix caching reuses the KV cache for a prompt beginning that many requests share.', why: 'System prompts and few-shot examples repeat on every call. Caching them removes most of the prefill cost.', example: 'A 1500 token system prompt is prefilled once. Later requests prefill only their 50 new tokens.', check: { q: 'Which requests gain most from prefix caching?', options: ['Ones with a long shared system prompt', 'Ones with very short prompts', 'Ones with random prompts'], answer: 0, explain: 'The shared prefix is computed once. The longer it is, the more prefill is skipped.' } },
    { id: 'd52-c12', term: 'Queueing and backpressure', plain: 'When requests arrive faster than the server finishes them, a queue forms and latency climbs.', why: 'An unbounded queue turns a burst into minutes of delay and memory exhaustion. Reject early with a 429 rather than accept and stall.', example: 'Cap the queue at 100 requests. The 101st gets a 429 with a retry-after header.', check: { q: 'What should a server do when its queue is full?', options: ['Accept and wait', 'Restart itself', 'Reject quickly with a retry signal'], answer: 2, explain: 'A fast rejection lets the client back off. Accepting only makes every request slower.' } },
    { id: 'd52-c13', term: 'Cold start', plain: 'A cold start is the delay when a new server must load weights before it can answer.', why: 'Scaling up under load is useless if each replica takes three minutes to load. Keep warm spares or pre-load from local disk.', example: 'Pulling 14 gigabytes from object storage takes 90 seconds. Loading from a local cache takes 15.', check: { q: 'How do you shorten cold starts?', options: ['Use a bigger model', 'Cache weights on local disk and warm up first', 'Reduce the batch size'], answer: 1, explain: 'Local weights load faster, and a warm-up request compiles kernels before real traffic arrives.' } },
    { id: 'd52-c14', term: 'Speculative decoding', plain: 'A small draft model guesses several tokens and the big model checks them in one pass.', why: 'Checking is one memory bound step for several tokens. Accepted guesses give the same output faster. Rejected ones cost little.', example: 'The draft proposes 5 tokens. The big model accepts 4. Five tokens for the price of about one step.', check: { q: 'Why is checking several tokens cheaper than generating them?', options: ['One weight read covers all the guesses', 'Checking uses a smaller model', 'Checked tokens are shorter'], answer: 0, explain: 'One forward pass verifies a whole draft. The weight read is shared across the guesses.' } },
    { id: 'd52-c15', term: 'Cost per token', plain: 'Serving cost per token is the hourly GPU price divided by the tokens produced in that hour.', why: 'This one number links every engineering choice to money. Batching, caching and quantisation all lower it.', example: 'A 2 pound an hour GPU making 1 million tokens an hour costs 2 pounds per million tokens.', check: { q: 'Which change lowers cost per token most directly?', options: ['A longer system prompt', 'Raising GPU utilisation through batching', 'Adding more logging'], answer: 1, explain: 'Same hourly price, more tokens per hour. Cost per token falls.' } },
  ],
  equations: [
    { id: 'd52-e01', name: 'Generation time', symbol: 'T = t₁ + n × tₜ', tokens: [{ sym: 'T', word: 'total-time' }, { sym: '=', word: 'is' }, { sym: 't₁', word: 'first-token-time' }, { sym: '+', word: 'plus' }, { sym: 'n', word: 'output-tokens' }, { sym: '×', word: 'times' }, { sym: 'tₜ', word: 'per-token-time' }], literal: 'total-time is first-token-time plus output-tokens times per-token-time', meaning: 'The user waits for prefill once, then for every output token. Long answers are dominated by the per-token cost.', example: '300 milliseconds first token, 200 tokens at 25 milliseconds each. Total is 5.3 seconds.', check: { q: 'First token at 500 milliseconds, then 100 tokens at 20 milliseconds. Total?', options: ['2.5 seconds', '0.7 seconds', '20.5 seconds'], answer: 0, explain: '100 times 20 is 2000 milliseconds. Plus 500 is 2500, which is 2.5 seconds.' } },
    { id: 'd52-e02', name: 'KV cache per token', symbol: 'K = 2 × L × d × b', tokens: [{ sym: 'K', word: 'cache-per-token' }, { sym: '=', word: 'is' }, { sym: '2', word: 'two' }, { sym: '×', word: 'times' }, { sym: 'L', word: 'layers' }, { sym: 'd', word: 'hidden-width' }, { sym: 'b', word: 'bytes-each' }], literal: 'cache-per-token is two times layers times hidden-width times bytes-each', meaning: 'Each layer keeps a key and a value vector per token. Multiply by context length and batch size for the total.', example: '2 times 32 layers times 4096 times 2 bytes is 524288 bytes, about half a megabyte.', check: { q: 'Cache is half a megabyte per token. How much for a 4000 token sequence?', options: ['200 megabytes', '2 gigabytes', '20 gigabytes'], answer: 1, explain: '4000 times 0.5 megabytes is 2000 megabytes, which is 2 gigabytes.' } },
    { id: 'd52-e03', name: 'Little’s law', symbol: 'N = λ × W', tokens: [{ sym: 'N', word: 'requests-in-system' }, { sym: '=', word: 'is' }, { sym: 'λ', word: 'arrival-rate' }, { sym: '×', word: 'times' }, { sym: 'W', word: 'time-per-request' }], literal: 'requests-in-system is arrival-rate times time-per-request', meaning: 'How many requests are in flight at once. It sets how many batch slots and how much cache you need.', example: '20 requests a second, each taking 2 seconds, means 40 in flight at any moment.', check: { q: '10 requests a second each lasting 5 seconds. How many in flight?', options: ['2', '15', '50'], answer: 2, explain: '10 times 5 is 50 requests in the system at once.' } },
    { id: 'd52-e04', name: 'Batch throughput', symbol: 'R = B / tₛ', tokens: [{ sym: 'R', word: 'tokens-per-second' }, { sym: '=', word: 'is' }, { sym: 'B', word: 'batch-size' }, { sym: '/', word: 'divided by' }, { sym: 'tₛ', word: 'step-time' }], literal: 'tokens-per-second is batch-size divided by step-time', meaning: 'Every decode step yields one token per sequence in the batch. A bigger batch at nearly the same step time multiplies output.', example: 'Batch 16 with a 25 millisecond step gives 640 tokens a second.', check: { q: 'Batch 32 and a 40 millisecond step. Tokens per second?', options: ['800', '80', '1280'], answer: 0, explain: '32 divided by 0.04 seconds is 800 tokens a second.' } },
    { id: 'd52-e05', name: 'Cost per token', symbol: 'C = h / T', tokens: [{ sym: 'C', word: 'cost-per-token' }, { sym: '=', word: 'is' }, { sym: 'h', word: 'hourly-price' }, { sym: '/', word: 'divided by' }, { sym: 'T', word: 'tokens-per-hour' }], literal: 'cost-per-token is hourly-price divided by tokens-per-hour', meaning: 'The price of the machine spread over its output. Idle capacity raises the cost of every token you do serve.', example: '3 pounds an hour and 1.5 million tokens an hour is 2 pounds per million tokens.', check: { q: 'Same GPU, double the tokens per hour. Cost per token?', options: ['Doubles', 'Halves', 'Unchanged'], answer: 1, explain: 'Same hourly price divided by twice the output. Half the cost per token.' } },
  ],
  problems: [
    { id: 'd52-p01', prompt: 'Time to first token is 300 milliseconds and each further token takes 25 milliseconds. How long for a 400 token answer?', hint: 'Multiply tokens by per-token time, then add the first token wait.', options: ['10.3 seconds', '1.3 seconds', '12 seconds'], answer: 0, worked: '400 times 25 milliseconds is 10 seconds. Add 0.3 seconds for the first token. Total is 10.3 seconds.' },
    { id: 'd52-p02', prompt: 'KV cache is half a megabyte per token. Context is 4096 tokens and the batch holds 32 requests. Cache memory needed?', hint: 'Per sequence first, then times the batch.', options: ['2 gigabytes', '64 gigabytes', '16 gigabytes'], answer: 1, worked: '4096 times 0.5 megabytes is 2 gigabytes per sequence. Times 32 sequences is 64 gigabytes.' },
  ],
  build: { id: 'd52-b01', title: 'Streaming token server', goal: 'Serve a streaming text endpoint and measure its first token and tail latency under load.', steps: ['Create a FastAPI POST endpoint that returns a StreamingResponse.', 'Yield one token every 20 milliseconds from a small model or word list.', 'Write a client that records time to first token and total time.', 'Send 50 requests with 8 in parallel and print p50 and p95.'], done: 'The client prints time to first token and p95 total latency across 50 requests.', check: { q: 'Why measure p95 rather than the average in the load test?', options: ['The tail is what slow users experience', 'Averages need more requests', 'p95 is easier to compute'], answer: 0, explain: 'One in twenty users sees the p95. The average hides them.' } },
  incident: { id: 'd52-i01', title: 'p99 latency spiked, p50 unchanged', signal: 'p50 latency holds at 400 milliseconds but p99 rose from 2 to 30 seconds. GPU memory reads full and the queue depth is climbing.', clue: 'A new customer started sending 30 thousand token prompts, and the input token limit was removed last week.', options: ['The GPU is faulty', 'DNS resolution is slow', 'A few very long prompts are hogging cache and slots'], answer: 2, explain: 'Long prompts fill the KV cache, so others queue behind them. Restore a per-request input cap, reserve cache per sequence, and route long prompts to a separate pool.' },
  research: { id: 'd52-r01', question: 'Does speculative decoding speed up your real traffic, or only benchmarks with predictable text?', hypothesis: 'On our chat traffic the draft acceptance rate exceeds 70 percent and decoding is at least 1.8 times faster.', method: 'Replay 1000 real prompts with and without the draft model. Record acceptance rate and tokens per second per prompt type.', check: { q: 'Why replay real prompts rather than a public benchmark?', options: ['Public benchmarks are copyrighted', 'Speed-up depends on how predictable your text is', 'Real prompts are shorter'], answer: 1, explain: 'Acceptance rate is text dependent. Only your traffic tells you your gain.' } },
  decision: { id: 'd52-l01', situation: 'Your chat product runs eight GPUs at 20 percent utilisation with p95 latency of 1.2 seconds. Finance wants serving cost cut by half this quarter.', options: ['Halve the GPUs and accept slower answers', 'Enable continuous batching, measure, then remove GPUs', 'Switch to a much smaller model'], answer: 1, reasoning: '20 percent utilisation means the GPUs are mostly idle. Continuous batching fills them without changing the model. Measure p95 after, then cut GPUs the numbers allow.' },
};

export default day;
