import type { Day } from '../types';

const day: Day = {
  day: 33,
  title: 'DNS and HTTP',
  goal: 'Follow a web request from a typed name to a returned page.',
  concepts: [
    { id: 'd33-c01', term: 'Domain name', plain: 'A domain name is a human readable name that stands for an address.', why: 'People remember words, machines need numbers. Every attack on names, from typosquats to hijacks, starts here.', example: 'example.com is a domain. Its A record points at 93.184.215.14.', check: { q: 'What does a domain name stand for?', options: ['A network address', 'A password', 'A file format'], answer: 0, explain: 'A domain name is a label that DNS turns into an address.' } },
    { id: 'd33-c02', term: 'URL', plain: 'A URL names a scheme, a host, a path and sometimes a query.', why: 'Reading a URL left to right tells you who you are talking to and what you are asking for.', example: 'https://shop.example.com/cart?id=7 has scheme https, host shop.example.com and path /cart.', check: { q: 'In https://a.example.org/login, what is the host?', options: ['https', 'a.example.org', '/login'], answer: 1, explain: 'The host sits between the scheme and the path. Here it is a.example.org.' } },
    { id: 'd33-c03', term: 'A record', plain: 'An A record maps a name to an IPv4 address. AAAA maps to IPv6.', why: 'This is the lookup that starts almost every connection. Change it and traffic goes elsewhere.', example: 'api.example.com A 203.0.113.10 sends API calls to that machine.', check: { q: 'What does an A record hold?', options: ['A mail server name', 'A text note', 'An IPv4 address'], answer: 2, explain: 'A records hold IPv4 addresses. AAAA records hold IPv6 addresses.' } },
    { id: 'd33-c04', term: 'CNAME', plain: 'A CNAME record says this name is an alias for another name.', why: 'Aliases point services at cloud providers. A dangling alias to a deleted target can be claimed by anyone.', example: 'www.example.com CNAME example.github.io means look up the second name instead.', check: { q: 'What does a CNAME record do?', options: ['Points a name at another name', 'Stores an IP address', 'Blocks a domain'], answer: 0, explain: 'A CNAME is an alias. The resolver follows it to the real name.' } },
    { id: 'd33-c05', term: 'Resolver', plain: 'A resolver is the server that looks up names on your behalf.', why: 'Whoever runs your resolver sees every site you visit and can answer falsely.', example: 'Your laptop asks 1.1.1.1 for example.com and 1.1.1.1 does the hunting.', check: { q: 'What does a resolver do?', options: ['Hosts your website', 'Finds addresses for names you ask about', 'Encrypts your traffic'], answer: 1, explain: 'The resolver chases the answer through the DNS tree and returns it to you.' } },
    { id: 'd33-c06', term: 'Authoritative server', plain: 'An authoritative server holds the official records for a domain.', why: 'Resolvers only cache. The authoritative server is the source of truth, so its security is the security of the name.', example: 'The name servers for example.com are a.iana-servers.net and b.iana-servers.net.', check: { q: 'Which server gives the official answer for a domain?', options: ['The resolver', 'The authoritative server', 'The web browser'], answer: 1, explain: 'Only the authoritative server holds the records. Everyone else repeats them.' } },
    { id: 'd33-c07', term: 'DNS caching', plain: 'Resolvers remember answers for a time to live set by the domain owner.', why: 'Short TTLs allow quick failover. Long TTLs cut load but keep a bad or old answer alive longer.', example: 'A record with TTL 300 is remembered for five minutes before being asked again.', check: { q: 'What does a DNS TTL of 3600 mean?', options: ['The record is checked every second', 'The domain expires in an hour', 'The answer may be cached for an hour'], answer: 2, explain: '3600 seconds is one hour. Resolvers may reuse the answer for that long.' } },
    { id: 'd33-c08', term: 'HTTP request', plain: 'An HTTP request is a method and path, then headers, then an optional body.', why: 'Every web attack and every API call is one of these. Reading raw requests is a core skill.', example: 'GET /index.html HTTP/1.1 followed by a Host header naming example.com.', check: { q: 'What comes first in an HTTP request?', options: ['The body', 'The method and path', 'The cookie'], answer: 1, explain: 'The request line with method, path and version comes first. Headers follow, then the body.' } },
    { id: 'd33-c09', term: 'HTTP methods', plain: 'The method says what the request wants. GET reads, POST creates, PUT replaces, DELETE removes.', why: 'Safe methods should not change state. A GET that deletes data is a bug attackers love.', example: 'GET /users/7 fetches a user. DELETE /users/7 removes them.', check: { q: 'Which method should never change server data?', options: ['GET', 'POST', 'DELETE'], answer: 0, explain: 'GET is defined as safe. Browsers and crawlers assume it only reads.' } },
    { id: 'd33-c10', term: 'Status codes', plain: 'A status code is a three digit number that says how the request went.', why: 'Codes in logs tell you what happened at scale. A burst of 401s or 500s is a signal.', example: '200 means fine, 404 means not found, 500 means the server broke.', check: { q: 'What does a 403 status mean?', options: ['Server error', 'Moved permanently', 'Forbidden'], answer: 2, explain: '403 is forbidden. The server understood the request but refuses it.' } },
    { id: 'd33-c11', term: 'Headers', plain: 'Headers are name and value lines that carry data about the request or response.', why: 'Headers hold hosts, content types, tokens and security policies. Missing or spoofed headers are common faults.', example: 'A Content-Type header of application/json tells the server the body is JSON.', check: { q: 'Which header tells the server which site was requested?', options: ['Host', 'Content-Length', 'Accept'], answer: 0, explain: 'One address can serve many sites. The Host header picks which one.' } },
    { id: 'd33-c12', term: 'Cookies', plain: 'A cookie is a small value the server sets and the browser sends back each time.', why: 'HTTP forgets everything between requests. Cookies carry the session, so stealing one steals the login.', example: 'The server sets session=abc123 with the HttpOnly and Secure flags.', check: { q: 'Why do servers use cookies?', options: ['To compress pages', 'To speed up DNS', 'To remember who you are between requests'], answer: 2, explain: 'HTTP is stateless. The cookie is how the server recognises the same browser again.' } },
    { id: 'd33-c13', term: 'HTTPS', plain: 'HTTPS is HTTP inside a TLS tunnel that encrypts and authenticates the connection.', why: 'Without it anyone on the path can read and rewrite pages. The certificate proves the host is who it claims.', example: 'The padlock means the certificate for example.com was checked and the traffic is encrypted.', check: { q: 'What does the TLS certificate prove?', options: ['The page has no bugs', 'The server owns the domain name', 'The user is logged in'], answer: 1, explain: 'A trusted authority signed the certificate for that name, so the server controls the domain.' } },
    { id: 'd33-c14', term: 'DNSSEC', plain: 'DNSSEC signs DNS records so a resolver can tell forged answers from real ones.', why: 'Plain DNS trusts whoever answers first. Signatures stop cache poisoning at the resolver.', example: 'A signed answer for example.com carries an RRSIG record the resolver verifies against a chain of keys.', check: { q: 'What does DNSSEC protect against?', options: ['Forged DNS answers', 'Slow web pages', 'Weak passwords'], answer: 0, explain: 'Signatures let the resolver reject answers that were not made by the domain owner.' } },
    { id: 'd33-c15', term: 'DNS tunnelling', plain: 'DNS tunnelling hides data inside query names to a domain the attacker controls.', why: 'DNS is almost always allowed out. Long random subdomains at high volume are the tell defenders look for.', example: 'A host sends 5,000 queries an hour for names like x7f3k9q2.evil-example.net.', check: { q: 'Which pattern most suggests DNS tunnelling?', options: ['A single lookup of example.com', 'A 404 error on a web page', 'Many long random subdomains to one domain'], answer: 2, explain: 'Data has to be encoded into names, so tunnels produce many unique random looking labels.' } },
  ],
  equations: [
    { id: 'd33-e01', name: 'Cache hit rate', symbol: 'H = h / (h + m)', tokens: [{ sym: 'H', word: 'hit-rate' }, { sym: '=', word: 'is' }, { sym: 'h', word: 'hits' }, { sym: '/', word: 'divided by' }, { sym: '+', word: 'plus' }, { sym: 'm', word: 'misses' }], literal: 'hit-rate is hits divided by hits plus misses', meaning: 'How often the resolver already knows the answer. High hit rates cut latency and shield the authoritative server from load.', example: '900 hits and 100 misses. 900 over 1000 is a hit-rate of 0.9.', check: { q: 'A cache sees 60 hits and 40 misses. What is the hit rate?', options: ['0.6', '0.4', '1.5'], answer: 0, explain: '60 divided by 100 total lookups is 0.6.' } },
    { id: 'd33-e02', name: 'Page round trips', symbol: 'T ≈ n·RTT', tokens: [{ sym: 'T', word: 'time-to-first-byte' }, { sym: '≈', word: 'is approximately' }, { sym: 'n', word: 'round-trips' }, { sym: '·', word: 'times' }, { sym: 'RTT', word: 'round-trip-time' }], literal: 'time-to-first-byte is approximately round-trips times round-trip-time', meaning: 'DNS, the TCP handshake, TLS and the request each need round trips. Distance multiplies through all of them.', example: 'Five round trips at 40 milliseconds each is about 200 milliseconds before the first byte.', check: { q: 'What cuts time-to-first-byte most on a long link?', options: ['A bigger server', 'Fewer round trips', 'More DNS records'], answer: 1, explain: 'Each round trip costs the full distance. Removing one saves a whole round-trip-time.' } },
    { id: 'd33-e03', name: 'Query load', symbol: 'Q = N / TTL', tokens: [{ sym: 'Q', word: 'queries-per-second' }, { sym: '=', word: 'is' }, { sym: 'N', word: 'clients' }, { sym: '/', word: 'divided by' }, { sym: 'TTL', word: 'time-to-live' }], literal: 'queries-per-second is clients divided by time-to-live', meaning: 'Each client re-asks once per time-to-live. Halving the TTL doubles the load on your name servers.', example: '10,000 clients and a 300 second TTL. About 33 queries per second reach the authoritative server.', check: { q: 'TTL drops from 600 to 60 seconds. What happens to query load?', options: ['It falls ten times', 'It stays the same', 'It rises ten times'], answer: 2, explain: 'Load is clients divided by TTL. Dividing by a tenth of the time gives ten times the load.' } },
    { id: 'd33-e04', name: 'Name entropy', symbol: 'H = −Σ p·log₂ p', tokens: [{ sym: 'H', word: 'entropy' }, { sym: '=', word: 'is' }, { sym: '−', word: 'minus' }, { sym: 'Σ', word: 'add-up' }, { sym: 'p', word: 'character-probability' }, { sym: '·', word: 'times' }, { sym: 'log₂', word: 'log-two' }], literal: 'entropy is minus add-up of character-probability times log-two of character-probability', meaning: 'Random looking names have high entropy. Detectors flag subdomains whose entropy is far above normal words.', example: 'The name google has entropy about 2.3 bits per character. A random string like x7f3k9q2 is near 3.', check: { q: 'Why would a detector score subdomain entropy?', options: ['To find randomly generated names', 'To speed up caching', 'To check the TTL'], answer: 0, explain: 'Generated names use characters evenly, so their entropy is higher than real words.' } },
    { id: 'd33-e05', name: 'Token bucket', symbol: 'R = r·t + b', tokens: [{ sym: 'R', word: 'allowed-requests' }, { sym: '=', word: 'is' }, { sym: 'r', word: 'refill-rate' }, { sym: '·', word: 'times' }, { sym: 't', word: 'seconds' }, { sym: '+', word: 'plus' }, { sym: 'b', word: 'bucket-size' }], literal: 'allowed-requests is refill-rate times seconds plus bucket-size', meaning: 'A rate limiter lets a burst through, then refills slowly. Requests beyond this get a 429 status.', example: 'Refill-rate 10 per second, bucket-size 50. In 5 seconds a client may make 100 requests.', check: { q: 'Refill 2 per second, bucket 20. How many requests in 10 seconds?', options: ['20', '40', '200'], answer: 1, explain: '2 times 10 is 20 refilled, plus the 20 in the bucket, gives 40.' } },
  ],
  problems: [
    { id: 'd33-p01', prompt: 'A resolver answered 8,000 of 10,000 lookups from cache. The rest went upstream. What is the hit rate?', hint: 'Hits divided by all lookups.', options: ['0.8', '0.2', '1.25'], answer: 0, worked: '8,000 divided by 10,000 is 0.8. Two lookups in ten had to leave the resolver.' },
    { id: 'd33-p02', prompt: 'Loading a page takes one round trip each for DNS, TCP and the request, plus two for TLS. RTT is 60 ms. Time to first byte?', hint: 'Count the round trips, then multiply.', options: ['180 ms', '300 ms', '240 ms'], answer: 1, worked: 'One plus one plus one plus two is five round trips. Five times 60 milliseconds is 300 milliseconds.' },
  ],
  build: { id: 'd33-b01', title: 'Site health checker', goal: 'Write a Python script that times DNS lookup and HTTP response for a list of sites.', steps: ['Read a list of hostnames from a text file.', 'Time socket.getaddrinfo for each name and record the address.', 'Time urllib.request.urlopen on https and record the status code.', 'Print a table of host, address, DNS time, HTTP time and status.'], done: 'The table shows five sites with times and codes, and a bad name shows an error.', check: { q: 'Why time DNS separately from the HTTP request?', options: ['DNS is always slower', 'So slow lookups are not blamed on the server', 'Because HTTP cannot be timed'], answer: 1, explain: 'Splitting the timings shows which stage is slow, so the fix goes to the right place.' } },
  incident: { id: 'd33-i01', title: 'Workstation sends thousands of DNS queries', signal: 'One laptop sent 42,000 DNS queries in an hour. All are for subdomains of one unfamiliar domain. The user was at lunch.', clue: 'The queried names are 50 to 60 random characters long and never repeat.', options: ['A browser prefetching links', 'Data leaving through DNS tunnelling', 'A misconfigured DNS cache'], answer: 1, explain: 'Long unique random names at high volume to one domain is tunnelling. Isolate the laptop, block the domain at the resolver and capture the process making the queries.' },
  research: { id: 'd33-r01', question: 'Does a shorter DNS TTL speed up failover enough to justify the extra resolver load?', hypothesis: 'Cutting TTL from 300 to 60 seconds cuts failover time by 80 percent and raises load five times.', method: 'Run both TTLs for a week each on a test domain. Measure queries per second and time for clients to reach a new address after a switch.', check: { q: 'What would make this comparison unfair?', options: ['Testing each TTL in a different week', 'Using the same domain', 'Measuring both failover and load'], answer: 0, explain: 'Traffic differs week to week. Alternate or run both at once so only the TTL changes.' } },
  decision: { id: 'd33-l01', situation: 'Your AI product calls a vendor model behind a single hostname. Last month a vendor DNS change caused a two hour outage. Engineers propose hard coding the vendor IP addresses.', options: ['Hard code the vendor IP addresses', 'Keep DNS, add health checks and a second vendor endpoint', 'Move to a different vendor'], answer: 1, reasoning: 'Hard coded addresses break the next time the vendor moves. Health checks with a failover endpoint survive both DNS and address changes. Switching vendors does not fix the design.' },
};

export default day;
