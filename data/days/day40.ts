import type { Day } from '../types';

const day: Day = {
  day: 40,
  title: 'Web security',
  goal: 'Protect sessions, pages and logins from the most common web attacks.',
  concepts: [
    { id: 'd40-c01', term: 'Origin', plain: 'An origin is the scheme, host and port of a URL taken together.', why: 'Browsers decide what a page may touch by origin. Everything in web security starts with this definition.', example: 'shop.example.com over HTTPS and api.example.com over HTTPS are different origins.', check: { q: 'Which two URLs share an origin?', options: ['Same host, one HTTP and one HTTPS', 'Same scheme, host and port, different paths', 'Same host, ports 80 and 8080'], answer: 1, explain: 'Path is not part of the origin. Scheme, host and port must all match.' } },
    { id: 'd40-c02', term: 'Same-origin policy', plain: 'A page may read responses only from its own origin unless the other origin explicitly allows it.', why: 'Without it, any site you visit could read your bank page open in another tab.', example: 'Script on evil.example cannot read your webmail inbox from another origin.', check: { q: 'What does the same-origin policy block?', options: ['Sending any request at all', 'Reading responses from another origin', 'Loading images'], answer: 1, explain: 'Requests can often be sent. Reading the response across origins is what the browser refuses.' } },
    { id: 'd40-c03', term: 'Cookies', plain: 'A cookie is a small value the browser sends back to the site that set it, on every request.', why: 'Cookies carry login sessions. Whoever holds the cookie is that user, so protecting cookies protects accounts.', example: 'After login the server sets session equals a long random string.', check: { q: 'What does the browser do with a cookie?', options: ['Deletes it after one request', 'Shares it with every site', 'Sends it back to the setting site'], answer: 2, explain: 'Cookies are scoped to the site that set them and returned automatically on each request.' } },
    { id: 'd40-c04', term: 'Cookie flags', plain: 'Secure sends a cookie only over HTTPS. HttpOnly hides it from page scripts.', why: 'Two flags remove two whole attack paths, sniffing on the wire and theft by injected script.', example: 'Set-Cookie session equals abc with Secure, HttpOnly and SameSite Lax.', check: { q: 'Which flag stops page JavaScript reading a cookie?', options: ['HttpOnly', 'Secure', 'Path'], answer: 0, explain: 'HttpOnly keeps the cookie out of document.cookie, so injected script cannot steal it.' } },
    { id: 'd40-c05', term: 'HTTPS and HSTS', plain: 'HTTPS encrypts traffic. HSTS tells the browser never to use plain HTTP for this site again.', why: 'One plain HTTP request can leak a cookie or be redirected. HSTS removes the first insecure request.', example: 'Strict-Transport-Security max-age 31536000 keeps the site HTTPS-only for a year.', check: { q: 'What does HSTS do?', options: ['Encrypts cookies', 'Compresses responses', 'Forces the browser to use HTTPS for the site'], answer: 2, explain: 'The browser remembers the header and upgrades every future request to HTTPS itself.' } },
    { id: 'd40-c06', term: 'Cross-site scripting', plain: 'XSS is when untrusted text is placed in a page and the browser runs it as script.', why: 'Injected script runs as the user, reading their data and acting for them. It is the most common web flaw.', example: 'A comment containing a script tag is shown unencoded to every visitor.', check: { q: 'What is the root cause of XSS?', options: ['Weak passwords', 'Untrusted text rendered as code', 'Slow servers'], answer: 1, explain: 'The browser cannot tell data from code once they are mixed in the page.' } },
    { id: 'd40-c07', term: 'HTML encoding', plain: 'Encode untrusted text for HTML before it enters the page, and let templates do it by default.', why: 'Encoding turns a script tag into visible text. Modern template engines do this automatically unless you switch it off.', example: 'Jinja2 auto-escapes by default. The safe filter turns that off and needs a review.', check: { q: 'Which template feature deserves a security review?', options: ['Auto-escaping turned off with a safe filter', 'A loop', 'A comment'], answer: 0, explain: 'Marking text safe hands the browser raw HTML. That is exactly where XSS enters.' } },
    { id: 'd40-c08', term: 'Content Security Policy', plain: 'CSP is a header that lists where scripts, styles and images may load from.', why: 'Even if injection slips through, CSP stops the browser running inline or foreign script. It is a second layer.', example: 'script-src self plus a nonce blocks every inline script the server did not stamp.', check: { q: 'What does CSP do if injected inline script appears?', options: ['The browser refuses to run it', 'It encrypts it', 'It reports and runs it'], answer: 0, explain: 'Script without the nonce or from an unlisted source is blocked before it executes.' } },
    { id: 'd40-c09', term: 'CSRF and SameSite', plain: 'CSRF tricks a logged-in browser into sending a request the user never intended.', why: 'Cookies are attached automatically, so a hidden form on another site could act as the user. SameSite and tokens stop this.', example: 'A SameSite Lax cookie is not sent on a cross-site POST, so the forged request has no session.', check: { q: 'What defence stops cookies riding along on cross-site POST requests?', options: ['Longer cookies', 'Gzip', 'SameSite attribute'], answer: 2, explain: 'SameSite Lax or Strict keeps the session cookie off cross-site form posts.' } },
    { id: 'd40-c10', term: 'CORS', plain: 'CORS headers let a server relax the same-origin policy for chosen origins.', why: 'APIs need to serve other origins, but allowing any origin with credentials hands data to any site.', example: 'The API returns Access-Control-Allow-Origin set to app.example.com only.', check: { q: 'Which CORS setting is dangerous with credentials?', options: ['Allow-Origin set to one named origin', 'Allow-Origin set to star', 'Max-Age of one hour'], answer: 1, explain: 'A wildcard with credentials lets any site read responses as the logged-in user.' } },
    { id: 'd40-c11', term: 'Clickjacking', plain: 'Clickjacking hides your site in an invisible frame so clicks land on buttons the user cannot see.', why: 'A framed page with a real session can be tricked into approving actions. Frame control blocks it.', example: 'CSP frame-ancestors none stops any other site framing your pages.', check: { q: 'Which header stops other sites framing your page?', options: ['Content-Type', 'Cache-Control', 'frame-ancestors in CSP'], answer: 2, explain: 'frame-ancestors, or the older X-Frame-Options, tells the browser who may embed the page.' } },
    { id: 'd40-c12', term: 'Broken access control', plain: 'The server must check that this user may see this object on every request.', why: 'Changing an id in the URL from 1041 to 1042 should not show another customer. Hiding links is not a check.', example: 'GET orders slash 1042 returns 403 unless the order belongs to the caller.', check: { q: 'A user changes the invoice id in a URL and sees another invoice. What failed?', options: ['Server-side authorisation', 'The TLS handshake', 'Browser caching'], answer: 0, explain: 'The server never checked ownership. Every object read needs an authorisation check.' } },
    { id: 'd40-c13', term: 'Credential stuffing', plain: 'Attackers replay username and password pairs leaked from other sites against your login.', why: 'People reuse passwords. Even a small reuse rate across millions of pairs yields thousands of accounts.', example: 'Two million pairs at half a percent reuse gives ten thousand compromised accounts.', check: { q: 'Best first defence against credential stuffing?', options: ['Longer session cookies', 'Hiding the login page', 'Rate limits, breach password checks and MFA'], answer: 2, explain: 'Cut the volume, refuse known-leaked passwords, and make a correct password insufficient.' } },
    { id: 'd40-c14', term: 'Server-side request forgery', plain: 'SSRF makes your server fetch a URL chosen by the attacker, often an internal one.', why: 'Servers sit inside the network. A fetch feature can reach metadata services and internal APIs the user never could.', example: 'An image preview feature is pointed at an internal address it should never reach.', check: { q: 'How do you defend a URL fetch feature?', options: ['Allow-list destinations and block internal ranges', 'Use a longer timeout', 'Fetch over HTTP only'], answer: 0, explain: 'Resolve the host, refuse private and metadata ranges, and allow only expected destinations.' } },
    { id: 'd40-c15', term: 'Subresource integrity', plain: 'SRI pins a script from a CDN to a hash, so a changed file is refused.', why: 'Third-party scripts run with your origin. If the CDN is compromised, the hash from day 38 stops the swap.', example: 'A script tag with integrity sha384 and a digest fails to load if the file changes.', check: { q: 'What happens when a CDN file no longer matches its SRI hash?', options: ['The browser refuses to run it', 'It loads with a warning', 'It is cached'], answer: 0, explain: 'The browser hashes the fetched file. A mismatch blocks execution entirely.' } },
  ],
  equations: [
    { id: 'd40-e01', name: 'Stuffing yield', symbol: 'A = N · s', tokens: [{ sym: 'A', word: 'accounts-taken' }, { sym: '=', word: 'is' }, { sym: 'N', word: 'pairs-tried' }, { sym: '·', word: 'times' }, { sym: 's', word: 'reuse-rate' }], literal: 'accounts-taken is pairs-tried times reuse-rate', meaning: 'Attackers do not need a high success rate. Volume does the work, so cutting volume with rate limits cuts the yield.', example: '1 million pairs times 0.005 gives 5000 accounts.', check: { q: 'Rate limiting cuts pairs-tried by 100 times. Accounts-taken?', options: ['Falls 100 times', 'Unchanged', 'Falls 2 times'], answer: 0, explain: 'Yield is proportional to pairs tried. Divide the tries by 100 and the yield falls by 100.' } },
    { id: 'd40-e02', name: 'Rate limit', symbol: 'n ≤ r · w', tokens: [{ sym: 'n', word: 'requests-allowed' }, { sym: '≤', word: 'is at most' }, { sym: 'r', word: 'rate' }, { sym: '·', word: 'times' }, { sym: 'w', word: 'window' }], literal: 'requests-allowed is at most rate times window', meaning: 'A limit is a rate over a window. Apply it per account and per address so a spread attack still hits the cap.', example: 'Rate 5 per minute over a 60 minute window allows at most 300 attempts.', check: { q: 'Limit is 10 per minute. How many login attempts in an hour?', options: ['10', '600', '60'], answer: 1, explain: '10 per minute times 60 minutes is 600.' } },
    { id: 'd40-e03', name: 'Token entropy', symbol: 'E = L · log₂ C', tokens: [{ sym: 'E', word: 'entropy-bits' }, { sym: '=', word: 'is' }, { sym: 'L', word: 'token-length' }, { sym: '·', word: 'times' }, { sym: 'log₂', word: 'log-two-of' }, { sym: 'C', word: 'alphabet-size' }], literal: 'entropy-bits is token-length times log-two-of alphabet-size', meaning: 'A session token must be unguessable. Length and alphabet together set the number of guesses an attacker faces.', example: '32 characters from 64 symbols is 32 times 6, which is 192 bits.', check: { q: 'A 16 character hex token has how many entropy bits?', options: ['16', '128', '64'], answer: 2, explain: 'Hex has 16 symbols, log two of 16 is 4. 16 times 4 is 64 bits.' } },
    { id: 'd40-e04', name: 'Guess chance', symbol: 'P = g / 2ᵇ', tokens: [{ sym: 'P', word: 'probability' }, { sym: '=', word: 'is' }, { sym: 'g', word: 'guesses' }, { sym: '/', word: 'divided by' }, { sym: '2ᵇ', word: 'two to the entropy-bits' }], literal: 'probability is guesses divided by two to the entropy-bits', meaning: 'Each extra bit halves the attacker chance. At 128 bits, a billion guesses a second for a century is still nothing.', example: '16 guesses against an 8-bit token is 16 divided by 256, which is 0.0625.', check: { q: 'Token entropy goes from 64 to 65 bits. Guess probability?', options: ['Halves', 'Doubles', 'Unchanged'], answer: 0, explain: 'One more bit doubles the space, so the same guesses cover half as much of it.' } },
    { id: 'd40-e05', name: 'HSTS lifetime', symbol: 'T = d · 86400', tokens: [{ sym: 'T', word: 'max-age-seconds' }, { sym: '=', word: 'is' }, { sym: 'd', word: 'days' }, { sym: '·', word: 'times' }, { sym: '86400', word: 'seconds-per-day' }], literal: 'max-age-seconds is days times seconds-per-day', meaning: 'HSTS max-age is in seconds. Start short while testing, then set a year or more and consider preload.', example: '365 days times 86400 is 31536000 seconds, the usual one year value.', check: { q: 'You want HSTS for 30 days. What max-age?', options: ['30', '2592000', '720'], answer: 1, explain: '30 times 86400 is 2592000 seconds.' } },
  ],
  problems: [
    { id: 'd40-p01', prompt: 'An attacker replays 200 thousand leaked pairs against your login. About 2 percent of your users reuse passwords. Expected compromised accounts?', hint: 'Multiply pairs by the reuse rate.', options: ['400', '4000', '40000'], answer: 1, worked: '200 thousand times 0.02 is 4000. That is why breached password checks and rate limits matter more than any single strong password.' },
    { id: 'd40-p02', prompt: 'A session id is 16 hexadecimal characters. Each hex character carries 4 bits. How many bits of entropy?', hint: 'Length times bits per character.', options: ['16 bits', '128 bits', '64 bits'], answer: 2, worked: '16 times 4 is 64 bits. That is weak for a session token. Use at least 128 bits from a cryptographic random source.' },
  ],
  build: { id: 'd40-b01', title: 'Security header checker', goal: 'Write a Python script that fetches your site and grades its security headers.', steps: ['Use requests to GET a URL you own and read response headers.', 'Check for CSP, HSTS, X-Content-Type-Options, X-Frame-Options and SameSite cookies.', 'Print present, missing or weak for each with a one-line reason.', 'Run it against staging in CI and fail on missing HSTS.'], done: 'The script reports every missing header on staging and passes on a hardened page.', check: { q: 'Why check the response after redirects rather than the first response?', options: ['The final page is what users receive', 'Redirects have no headers', 'It is faster'], answer: 0, explain: 'Users end up on the final page. Its headers are the ones that protect them.' } },
  incident: { id: 'd40-i01', title: 'Login failures spike from thousands of addresses', signal: 'Failed logins rose fifty times overnight. Attempts come from thousands of addresses, each trying a few different usernames once.', clue: 'Most attempted usernames appear in a public breach list from another company.', options: ['A bug in the password form', 'Credential stuffing with a leaked list', 'A monitoring agent misfiring'], answer: 1, explain: 'The spread and the breach match mean stuffing. Rate limit per account, block breached passwords, force MFA or resets on matched accounts, and watch for successes.' },
  research: { id: 'd40-r01', question: 'Does deploying CSP in report-only mode first find most violations before enforcement breaks pages?', hypothesis: 'Two weeks of report-only mode captures over 90 percent of violation types seen in the first enforced month.', method: 'Run report-only for two weeks and log violations. Enforce, then compare new violation types over the next month.', check: { q: 'What would refute the hypothesis?', options: ['Many new violation types after enforcement', 'Zero violations in report-only mode', 'Pages loading faster'], answer: 0, explain: 'If enforcement reveals many types the trial missed, report-only did not capture most of them.' } },
  decision: { id: 'd40-l01', situation: 'Your chat product renders LLM answers into the page. Designers want rich formatting. The model can be steered by documents it reads.', options: ['Insert model output as raw HTML', 'Render a safe markdown subset to text nodes under strict CSP', 'Ban all formatting'], answer: 1, reasoning: 'Model output is untrusted like any user text, and prompt injection can put script in it. A sanitised subset with CSP gives formatting safely. Raw HTML is stored XSS with extra steps.' },
};

export default day;
