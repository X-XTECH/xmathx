import type { Day } from '../types';

const day: Day = {
  day: 50,
  title: 'GPUs',
  goal: 'Work out whether a model fits a GPU and what limits its speed.',
  concepts: [
    { id: 'd50-c01', term: 'GPU', plain: 'A GPU is a chip with thousands of small cores that all do the same sum at once.', why: 'Language models are mostly matrix multiplication, which is millions of identical little sums. A GPU does them together instead of one after another.', example: 'An H100 has over sixteen thousand cores. A laptop CPU has eight.', check: { q: 'What makes a GPU fast for AI work?', options: ['Thousands of cores doing the same sum', 'A very high clock speed', 'A larger hard disk'], answer: 0, explain: 'Model maths is many identical sums. Thousands of cores do them at the same time.' } },
    { id: 'd50-c02', term: 'CPU versus GPU', plain: 'A CPU has a few fast cores for logic. A GPU has many slower cores for bulk arithmetic.', why: 'Serving a model uses both. The CPU handles requests, parsing and branching. The GPU handles the heavy multiplication.', example: 'Tokenising the prompt runs on the CPU. Multiplying the weights runs on the GPU.', check: { q: 'Which job suits a CPU better than a GPU?', options: ['Multiplying two huge matrices', 'Parsing a JSON request', 'Running an attention layer'], answer: 1, explain: 'Parsing is branching logic on small data. That is CPU work. Bulk arithmetic belongs on the GPU.' } },
    { id: 'd50-c03', term: 'Parallel work', plain: 'Parallel work splits one big job into many identical small jobs done at the same time.', why: 'GPU speed comes from splitting, not from doing each piece faster. If a job cannot be split, the cores sit idle.', example: 'Adding two vectors of a million numbers. Each core adds one pair.', check: { q: 'When does a GPU give almost no speed-up?', options: ['When the vectors are very large', 'When the numbers are decimals', 'When each step depends on the previous one'], answer: 2, explain: 'A chain of dependent steps cannot be split. Only one core can work at a time.' } },
    { id: 'd50-c04', term: 'GPU memory', plain: 'GPU memory, called VRAM or HBM, holds the model weights and the working values.', why: 'A model must fit in GPU memory to run at full speed. This is the first number to check before choosing hardware.', example: 'An H100 has 80 gigabytes. A consumer RTX 4090 has 24 gigabytes.', check: { q: 'What is the first check before running a model on a GPU?', options: ['Whether the weights fit in GPU memory', 'Whether the fan is loud', 'Whether the CPU has many cores'], answer: 0, explain: 'If the weights do not fit, nothing else matters. Memory size is the first gate.' } },
    { id: 'd50-c05', term: 'Precision', plain: 'Precision is how many bytes store each number. FP32 uses 4, FP16 and BF16 use 2.', why: 'Halving the bytes halves the memory and doubles the numbers moved per second. Most inference runs in 16 bit with little loss.', example: 'A 7 billion parameter model is 28 gigabytes in FP32 and 14 in BF16.', check: { q: 'What happens to memory when you move from FP32 to BF16?', options: ['It doubles', 'It halves', 'It stays the same'], answer: 1, explain: 'FP32 is 4 bytes per number and BF16 is 2. Half the bytes, half the memory.' } },
    { id: 'd50-c06', term: 'Memory bandwidth', plain: 'Bandwidth is how many bytes per second move between GPU memory and the cores.', why: 'Generating one token reads every weight once. For that job, bandwidth sets the speed, not the number of cores.', example: 'An H100 moves about 3.35 terabytes per second. A 4090 moves about 1.', check: { q: 'What sets the speed of generating one token at a time?', options: ['Disk speed', 'Memory bandwidth', 'Number of CPU cores'], answer: 1, explain: 'Each token reads all the weights from memory. How fast they can be read is the limit.' } },
    { id: 'd50-c07', term: 'Host to device copy', plain: 'Data must cross the PCIe bus from CPU memory to GPU memory before the GPU can use it.', why: 'PCIe is far slower than GPU memory. Copying inputs every step can cost more than the maths itself.', example: 'PCIe 5 moves about 64 gigabytes a second. GPU memory moves fifty times more.', check: { q: 'Why keep data on the GPU between steps?', options: ['The CPU cannot read GPU memory', 'PCIe copies are slow compared with GPU memory', 'GPU memory is cheaper'], answer: 1, explain: 'Each copy across PCIe is slow. Keeping tensors on the device avoids repeating it.' } },
    { id: 'd50-c08', term: 'Kernel', plain: 'A kernel is a small function that runs on every GPU core at once.', why: 'PyTorch turns your code into kernel launches. Each launch has a fixed cost, so many tiny kernels waste time.', example: 'One kernel multiplies a whole matrix. A Python loop of tiny ops launches thousands.', check: { q: 'Why is a Python loop over tensor rows slow on a GPU?', options: ['Python cannot see the GPU', 'The GPU only runs once a second', 'Each loop step launches its own kernel'], answer: 2, explain: 'Every launch has fixed overhead. One big kernel beats thousands of small ones.' } },
    { id: 'd50-c09', term: 'Tensor cores', plain: 'Tensor cores are hardware units that multiply small matrix blocks in a single step.', why: 'Nearly all of the peak speed of a modern GPU comes from tensor cores, and only in 16 bit or lower precision.', example: 'An H100 does about 990 trillion BF16 operations a second, but only 67 in FP32.', check: { q: 'How do you get the peak speed of a modern GPU?', options: ['Use 16 bit or lower matrix multiplication', 'Use FP32 everywhere', 'Use more CPU threads'], answer: 0, explain: 'Tensor cores only accelerate low precision matrix maths. FP32 misses them.' } },
    { id: 'd50-c10', term: 'Batching', plain: 'Batching runs many inputs through the model in one pass, so the weights are read once.', why: 'Reading the weights is the expensive part. Sharing that read across a batch multiplies throughput for almost no extra time.', example: 'One request takes 20 milliseconds. Eight requests together take 22.', check: { q: 'Why does a batch of eight cost almost the same as one?', options: ['The GPU skips seven requests', 'The weights are read once for all eight', 'Batches use less precision'], answer: 1, explain: 'The weight read dominates. Doing it once for eight inputs is nearly free.' } },
    { id: 'd50-c11', term: 'Utilisation', plain: 'Utilisation is the share of time the GPU cores are actually busy.', why: 'An idle GPU costs the same as a busy one. Low utilisation means you are paying for waiting.', example: 'nvidia-smi shows 15 percent. The GPU is waiting on the CPU or the network.', check: { q: 'What does 15 percent GPU utilisation usually mean?', options: ['The model is too small', 'The GPU is broken', 'The GPU is waiting for work'], answer: 2, explain: 'The cores are idle most of the time. Something upstream is not feeding them.' } },
    { id: 'd50-c12', term: 'Memory bound', plain: 'A job is memory bound when cores wait for data, and compute bound when data waits for cores.', why: 'Knowing which one you are tells you what to fix. More cores do nothing for a memory bound job.', example: 'Decoding one token is memory bound. Training a large batch is compute bound.', check: { q: 'What helps a memory bound job?', options: ['A faster memory or smaller weights', 'More tensor cores', 'A bigger disk'], answer: 0, explain: 'The cores are starved. Move fewer bytes or move them faster.' } },
    { id: 'd50-c13', term: 'Roofline', plain: 'The roofline caps speed at peak compute or at bandwidth times work per byte, whichever is lower.', why: 'It shows on one chart whether a workload is limited by cores or by memory, and how far it is from either limit.', example: 'One flop per byte at 3 terabytes a second gives 3 teraflops, far under the 990 peak.', check: { q: 'What does the roofline compare?', options: ['Price against speed', 'Work per byte against the two hardware limits', 'Training against inference'], answer: 1, explain: 'It plots work per byte moved against the compute ceiling and the bandwidth slope.' } },
    { id: 'd50-c14', term: 'Quantisation', plain: 'Quantisation stores weights in fewer bits, such as 8 bit or 4 bit integers.', why: 'Fewer bits means less memory and fewer bytes per token, so larger models fit and decode faster, at a small cost in quality.', example: 'A 70 billion parameter model at 4 bits is about 35 gigabytes and fits one 80 gigabyte GPU.', check: { q: 'What is the main gain from 4 bit weights?', options: ['Higher accuracy', 'Faster training', 'Less memory and faster decoding'], answer: 2, explain: 'Fewer bytes to store and to read each token. Accuracy usually drops slightly.' } },
    { id: 'd50-c15', term: 'Multi-GPU splitting', plain: 'When a model does not fit one GPU, it is split across several by layer or by tensor.', why: 'Tensor splitting needs a fast link like NVLink because GPUs exchange data every layer. Layer splitting needs less bandwidth but leaves GPUs idle.', example: 'A 405 billion parameter model in BF16 is 810 gigabytes and needs at least eleven 80 gigabyte GPUs.', check: { q: 'Why does tensor splitting need NVLink rather than PCIe?', options: ['GPUs exchange data on every layer', 'PCIe cannot carry model weights', 'NVLink is cheaper'], answer: 0, explain: 'Each layer needs partial results from every GPU. A slow link makes every layer wait.' } },
  ],
  equations: [
    { id: 'd50-e01', name: 'Model memory', symbol: 'M = N × b', tokens: [{ sym: 'M', word: 'model-memory' }, { sym: '=', word: 'is' }, { sym: 'N', word: 'parameter-count' }, { sym: '×', word: 'times' }, { sym: 'b', word: 'bytes-each' }], literal: 'model-memory is parameter-count times bytes-each', meaning: 'The bytes needed for the weights alone. Add room for working values and cache on top.', example: '7 billion parameters times 2 bytes is 14 billion bytes, about 14 gigabytes.', check: { q: 'How much memory do 13 billion parameters need at 2 bytes each?', options: ['13 gigabytes', '26 gigabytes', '52 gigabytes'], answer: 1, explain: '13 billion times 2 is 26 billion bytes, about 26 gigabytes.' } },
    { id: 'd50-e02', name: 'Work per token', symbol: 'F ≈ 2N', tokens: [{ sym: 'F', word: 'flops-per-token' }, { sym: '≈', word: 'is approximately' }, { sym: '2N', word: 'two times parameter-count' }], literal: 'flops-per-token is approximately two times parameter-count', meaning: 'Each parameter costs one multiply and one add per token during inference.', example: '7 billion parameters gives about 14 billion floating point operations for each token.', check: { q: 'Roughly how many operations does a 70 billion parameter model spend per token?', options: ['70 billion', '140 billion', '7 billion'], answer: 1, explain: 'Two operations per parameter. 2 times 70 billion is 140 billion.' } },
    { id: 'd50-e03', name: 'Arithmetic intensity', symbol: 'I = F / B', tokens: [{ sym: 'I', word: 'intensity' }, { sym: '=', word: 'is' }, { sym: 'F', word: 'flops' }, { sym: '/', word: 'divided by' }, { sym: 'B', word: 'bytes-moved' }], literal: 'intensity is flops divided by bytes-moved', meaning: 'How much maths you get for every byte read. Low intensity means the job is memory bound.', example: '14 billion flops divided by 14 billion bytes is 1 flop per byte. Very memory bound.', check: { q: 'What does batching do to arithmetic intensity?', options: ['Lowers it', 'Leaves it unchanged', 'Raises it'], answer: 2, explain: 'Same bytes read, more flops done. More maths per byte means higher intensity.' } },
    { id: 'd50-e04', name: 'Decode step time', symbol: 't = M / W', tokens: [{ sym: 't', word: 'step-time' }, { sym: '=', word: 'is' }, { sym: 'M', word: 'model-memory' }, { sym: '/', word: 'divided by' }, { sym: 'W', word: 'bandwidth' }], literal: 'step-time is model-memory divided by bandwidth', meaning: 'The fastest one token can be produced. Every weight must be read from memory each step.', example: '14 gigabytes divided by 3.35 terabytes a second is about 4 milliseconds, so about 240 tokens a second.', check: { q: 'Weights are 40 gigabytes and bandwidth is 2 terabytes a second. Best step time?', options: ['20 milliseconds', '2 milliseconds', '200 milliseconds'], answer: 0, explain: '40 divided by 2000 is 0.02 seconds, which is 20 milliseconds.' } },
    { id: 'd50-e05', name: 'Model flops utilisation', symbol: 'U = Fₐ / Fₚ', tokens: [{ sym: 'U', word: 'utilisation' }, { sym: '=', word: 'is' }, { sym: 'Fₐ', word: 'achieved-flops' }, { sym: '/', word: 'divided by' }, { sym: 'Fₚ', word: 'peak-flops' }], literal: 'utilisation is achieved-flops divided by peak-flops', meaning: 'The fraction of the chip you are really using. Training runs reach 40 to 50 percent. Decoding is often under 10.', example: '400 trillion achieved over 990 trillion peak is about 0.40, or 40 percent.', check: { q: 'A GPU peaks at 1000 teraflops and achieves 50. What is utilisation?', options: ['50 percent', '5 percent', '20 percent'], answer: 1, explain: '50 divided by 1000 is 0.05, which is 5 percent.' } },
  ],
  problems: [
    { id: 'd50-p01', prompt: 'A 70 billion parameter model must run in BF16 on 80 gigabyte GPUs. How many GPUs do the weights alone need?', hint: 'Two bytes per parameter, then divide by 80 and round up.', options: ['One', 'Two', 'Four'], answer: 1, worked: '70 billion times 2 bytes is 140 gigabytes. 140 divided by 80 is 1.75, so round up to two GPUs.' },
    { id: 'd50-p02', prompt: 'Weights are 40 gigabytes and bandwidth is 2 terabytes a second. What is the best case tokens per second for one request?', hint: 'Find the step time first, then take one over it.', options: ['20 tokens a second', '500 tokens a second', '50 tokens a second'], answer: 2, worked: '40 divided by 2000 is 0.02 seconds per token. One divided by 0.02 is 50 tokens a second.' },
  ],
  build: { id: 'd50-b01', title: 'GPU fit calculator', goal: 'Write a script that says whether a model fits a given GPU and how fast it can decode.', steps: ['Read parameter count, bytes per weight and GPU memory from arguments.', 'Multiply parameters by bytes to get weights memory in gigabytes.', 'Add 20 percent for working values, then compare with GPU memory.', 'Divide weights memory by bandwidth and print tokens per second.'], done: 'Running it for 70 billion at 2 bytes reports 140 gigabytes and two 80 gigabyte GPUs.', check: { q: 'Why add a margin above the weights memory?', options: ['Weights grow during inference', 'Working values and cache need memory too', 'GPUs report their memory wrongly'], answer: 1, explain: 'Activations and the attention cache take space beyond the weights. Leave room for them.' } },
  incident: { id: 'd50-i01', title: 'Throughput collapsed after deploy', signal: 'After a deploy, tokens per second fell from 40 to 3. GPU utilisation reads 0 percent while the CPU is pinned at 100. The model file is unchanged.', clue: 'The new container image installed the CPU-only PyTorch wheel instead of the CUDA build.', options: ['The model is running on the CPU, not the GPU', 'The GPU has overheated', 'The network link is saturated'], answer: 0, explain: 'Without the CUDA build, torch cannot see the GPU and falls back to CPU. Pin the CUDA wheel and add a start-up check that fails when no GPU is found.' },
  research: { id: 'd50-r01', question: 'Does 4 bit quantisation hurt answer quality on your own tasks, or only on public benchmarks?', hypothesis: 'The 4 bit model scores within one point of the 16 bit model on our evaluation set.', method: 'Run the same 500 prompts through both models. Score with the same grader. Compare the averages with confidence intervals.', check: { q: 'Why score both models with the same grader?', options: ['It is faster', 'So the only difference is the precision', 'Graders are expensive'], answer: 1, explain: 'Changing the grader would add a second difference. Hold everything fixed except the thing under test.' } },
  decision: { id: 'd50-l01', situation: 'Your team wants to buy eight H100s for 250 thousand pounds. Today a 7 billion parameter model serves 200 requests a day from one rented cloud GPU.', options: ['Buy the eight GPUs now', 'Measure utilisation on rented GPUs first', 'Move everything to CPUs'], answer: 1, reasoning: '200 requests a day barely uses one GPU. Buying eight locks capital into idle hardware. Rent, measure utilisation and latency, and buy only when the numbers demand it.' },
};

export default day;
