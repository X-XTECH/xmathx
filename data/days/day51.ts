import type { Day } from '../types';

const day: Day = {
  day: 51,
  title: 'Data pipelines',
  goal: 'Build a pipeline that can be re-run safely and checked before training.',
  concepts: [
    { id: 'd51-c01', term: 'Data pipeline', plain: 'A data pipeline is a chain of steps moving data from where it is made to where it is used.', why: 'Models are only as good as the data that reaches them. Most production failures start in the pipeline, not the model.', example: 'App logs go to storage, get cleaned, and land in a training table every night.', check: { q: 'What is a data pipeline?', options: ['A chain of steps moving and shaping data', 'A type of database', 'A model training loop'], answer: 0, explain: 'It is the path data takes from source to use, with cleaning and shaping along the way.' } },
    { id: 'd51-c02', term: 'Source and sink', plain: 'A source is where data comes from. A sink is where the pipeline writes it.', why: 'Naming both ends makes a pipeline easy to draw and easy to test. Every step has an input and an output.', example: 'Source is a Postgres orders table. Sink is a Parquet file in object storage.', check: { q: 'In a pipeline, what is a sink?', options: ['The step that cleans data', 'Where the data is written', 'The schedule'], answer: 1, explain: 'The sink is the destination. The source is the origin.' } },
    { id: 'd51-c03', term: 'Batch processing', plain: 'Batch processing collects data over a period and then processes it all at once.', why: 'Batches are simple, cheap and easy to retry. Most training data pipelines are nightly batches.', example: 'Every night at 2 am, yesterday’s events are loaded and summarised.', check: { q: 'What is the main strength of batch processing?', options: ['Data is ready within seconds', 'It needs no storage', 'It is simple and easy to retry'], answer: 2, explain: 'A batch is a fixed chunk. If it fails, you run the same chunk again.' } },
    { id: 'd51-c04', term: 'Streaming', plain: 'Streaming processes each event as it arrives instead of waiting for a batch.', why: 'Use it when freshness matters, such as fraud checks. It costs more to build and to keep correct.', example: 'A Kafka topic receives each card payment and a consumer scores it within a second.', check: { q: 'When is streaming worth the extra complexity?', options: ['When results are needed within seconds', 'When data arrives once a day', 'When the data is small'], answer: 0, explain: 'Streaming buys freshness. If an hour of delay is fine, batch is cheaper.' } },
    { id: 'd51-c05', term: 'Extract transform load', plain: 'ETL extracts data, transforms it, then loads it. ELT loads first and transforms inside the warehouse.', why: 'ELT keeps the raw data, so you can redo a transform later. ETL saves storage but loses the original.', example: 'ELT copies raw JSON into BigQuery, then SQL builds clean tables from it.', check: { q: 'What is the advantage of loading raw data before transforming it?', options: ['It uses less storage', 'You can redo the transform later', 'It runs faster'], answer: 1, explain: 'Raw data kept means a bug in the transform is fixable by re-running it.' } },
    { id: 'd51-c06', term: 'Schema', plain: 'A schema states the columns, their types and which ones are required.', why: 'A pipeline with a written schema can reject bad rows early. Without one, a renamed column silently becomes nulls.', example: 'user_id integer required, amount decimal required, note string optional.', check: { q: 'What does a schema let a pipeline do?', options: ['Run without storage', 'Train models faster', 'Reject rows with wrong or missing columns'], answer: 2, explain: 'A schema is a contract. Rows that break it are caught at the door.' } },
    { id: 'd51-c07', term: 'Idempotence', plain: 'An idempotent step gives the same result whether it runs once or ten times.', why: 'Jobs get retried. If a retry inserts rows again, your training data doubles and nobody notices.', example: 'Upsert on the primary key instead of plain insert. Running twice leaves one copy.', check: { q: 'Which load is idempotent?', options: ['Append every row each run', 'Upsert on the primary key', 'Insert with a random id'], answer: 1, explain: 'Upsert replaces on the key, so repeating the run changes nothing.' } },
    { id: 'd51-c08', term: 'Partitioning', plain: 'Partitioning splits a large table into chunks, usually one per day.', why: 'You can reload one day without touching the rest, and queries that filter by date read only what they need.', example: 'Files stored as events/date=2026-09-03/part-0.parquet.', check: { q: 'Why partition a table by date?', options: ['Reload or query one day at a time', 'Dates compress better', 'It makes rows unique'], answer: 0, explain: 'A partition is a unit of work. One bad day can be replaced alone.' } },
    { id: 'd51-c09', term: 'Backfill', plain: 'A backfill re-runs the pipeline over past dates to fill gaps or apply a fix.', why: 'Bugs get found late. Backfills are only safe when steps are idempotent and partitioned.', example: 'A transform bug from March is fixed. Re-run March partitions only.', check: { q: 'What makes a backfill safe?', options: ['Running it at night', 'Idempotent steps over date partitions', 'Using more workers'], answer: 1, explain: 'Each date is replaced cleanly. Repeating a day cannot double it.' } },
    { id: 'd51-c10', term: 'Orchestration', plain: 'An orchestrator runs pipeline steps in order, on a schedule, and retries failures.', why: 'Steps depend on each other. A tool like Airflow draws them as a directed graph and knows what to re-run.', example: 'Extract runs at 2 am. Transform waits for it. Load waits for transform.', check: { q: 'What does an orchestrator add over a cron job?', options: ['Dependencies between steps and retries', 'Faster SQL', 'Bigger storage'], answer: 0, explain: 'Cron starts things on time. An orchestrator knows what depends on what.' } },
    { id: 'd51-c11', term: 'Data validation', plain: 'Validation checks each batch against rules before it is used.', why: 'A row count that halves, a null rate that jumps, or a new category are all signs of an upstream break.', example: 'Fail the run if row count is under half of yesterday or null user_id exceeds 1 percent.', check: { q: 'Where should validation sit?', options: ['After the model is trained', 'Before the data is used for training', 'Only in tests'], answer: 1, explain: 'Catching bad data before training saves a wasted run and a bad model.' } },
    { id: 'd51-c12', term: 'Deduplication', plain: 'Deduplication removes repeated rows that describe the same event.', why: 'Retries, replays and double clicks create duplicates. Duplicates bias counts and let the model see test rows twice.', example: 'Keep the latest row for each event id and drop the rest.', check: { q: 'Why do duplicates matter for training?', options: ['They use more disk', 'They make training slower', 'They bias counts and leak repeated rows'], answer: 2, explain: 'Repeated rows carry extra weight and can appear on both sides of a split.' } },
    { id: 'd51-c13', term: 'Lineage', plain: 'Lineage records which sources and code produced each table and each model.', why: 'When a model goes wrong you need to trace back to the exact data version. Without lineage, you guess.', example: 'Model v12 trained on events snapshot 2026-09-01 built by transform commit a1b2c3.', check: { q: 'What question does lineage answer?', options: ['How fast was the pipeline', 'Which data and code produced this output', 'Who is on call'], answer: 1, explain: 'Lineage is the paper trail from source to model.' } },
    { id: 'd51-c14', term: 'Late data', plain: 'Late data arrives after the batch for its period has already run.', why: 'Mobile devices send events hours later. If yesterday’s partition is final at 2 am, those rows are lost or double counted.', example: 'A watermark waits until 6 am before closing yesterday, then reprocesses stragglers weekly.', check: { q: 'How do pipelines handle late arriving events?', options: ['Drop them', 'Wait for a watermark and reprocess later', 'Assign them to today'], answer: 1, explain: 'A watermark delays closing a period. A later re-run picks up stragglers.' } },
    { id: 'd51-c15', term: 'Training serving skew', plain: 'Skew is when features computed for training differ from those computed at serving time.', why: 'Two code paths drift apart. The model learns one thing and is asked another. A shared feature store removes the gap.', example: 'Training uses average spend over 30 days. Serving uses 28 days by mistake.', check: { q: 'What removes training serving skew?', options: ['A bigger model', 'More training data', 'One feature definition used by both paths'], answer: 2, explain: 'One definition, one code path. Both sides compute the same number.' } },
  ],
  equations: [
    { id: 'd51-e01', name: 'Throughput', symbol: 'R = n / t', tokens: [{ sym: 'R', word: 'rate' }, { sym: '=', word: 'is' }, { sym: 'n', word: 'rows' }, { sym: '/', word: 'divided by' }, { sym: 't', word: 'time' }], literal: 'rate is rows divided by time', meaning: 'How many rows a step handles each second. The slowest step sets the pipeline rate.', example: '3 million rows in 600 seconds is 5000 rows a second.', check: { q: 'A step does 120 thousand rows in 60 seconds. What is its rate?', options: ['2000 rows a second', '200 rows a second', '7200 rows a second'], answer: 0, explain: '120 thousand divided by 60 is 2000 rows a second.' } },
    { id: 'd51-e02', name: 'Pipeline time', symbol: 'T = Σ tᵢ', tokens: [{ sym: 'T', word: 'total-time' }, { sym: '=', word: 'is' }, { sym: 'Σ', word: 'add-up of' }, { sym: 'tᵢ', word: 'each step-time' }], literal: 'total-time is add-up of each step-time', meaning: 'Steps that run one after another add their times. Steps that run side by side do not.', example: 'Extract 10 minutes, transform 25, load 5. Total is 40 minutes.', check: { q: 'Extract takes 10 minutes and two independent transforms take 20 each. Total if run together?', options: ['50 minutes', '30 minutes', '20 minutes'], answer: 1, explain: 'The two transforms run side by side, so 10 plus 20, not 10 plus 40.' } },
    { id: 'd51-e03', name: 'Data lag', symbol: 'L = tₙ − tₑ', tokens: [{ sym: 'L', word: 'lag' }, { sym: '=', word: 'is' }, { sym: 'tₙ', word: 'now' }, { sym: '−', word: 'minus' }, { sym: 'tₑ', word: 'newest event-time' }], literal: 'lag is now minus newest event-time', meaning: 'How stale the data is. A rising lag means the pipeline is falling behind or has stopped.', example: 'It is 9 am and the newest row is from 3 am. Lag is 6 hours.', check: { q: 'Lag was 1 hour yesterday and 9 hours today. What does that suggest?', options: ['The data got bigger', 'The pipeline has stalled or fallen behind', 'Users went quiet'], answer: 1, explain: 'Lag measures staleness. A jump means new data is not landing.' } },
    { id: 'd51-e04', name: 'Storage growth', symbol: 'S = r × d × b', tokens: [{ sym: 'S', word: 'storage' }, { sym: '=', word: 'is' }, { sym: 'r', word: 'rows-per-day' }, { sym: '×', word: 'times' }, { sym: 'd', word: 'days' }, { sym: 'b', word: 'bytes-per-row' }], literal: 'storage is rows-per-day times days times bytes-per-row', meaning: 'How much space a table needs after a period. Plan retention before the disk fills.', example: '1 million rows a day, 365 days, 100 bytes each is 36.5 gigabytes.', check: { q: '2 million rows a day at 500 bytes for 30 days. How much storage?', options: ['3 gigabytes', '300 gigabytes', '30 gigabytes'], answer: 2, explain: '2 million times 500 is 1 gigabyte a day. Times 30 is 30 gigabytes.' } },
    { id: 'd51-e05', name: 'Parallel batch time', symbol: 'T = n / (w × R)', tokens: [{ sym: 'T', word: 'time' }, { sym: '=', word: 'is' }, { sym: 'n', word: 'rows' }, { sym: '/', word: 'divided by' }, { sym: 'w', word: 'workers' }, { sym: '×', word: 'times' }, { sym: 'R', word: 'rate-per-worker' }], literal: 'time is rows divided by, workers times rate-per-worker', meaning: 'Splitting rows across workers divides the time, as long as the rows are independent.', example: '10 million rows, 4 workers at 5000 rows a second each. Time is 500 seconds.', check: { q: 'Doubling workers on independent rows does what to batch time?', options: ['Halves it', 'Doubles it', 'Nothing'], answer: 0, explain: 'Twice the workers, twice the combined rate, half the time.' } },
  ],
  problems: [
    { id: 'd51-p01', prompt: 'A pipeline lands 3 million rows a day at 200 bytes each. How much storage after one year?', hint: 'Bytes per day first, then times 365.', options: ['219 gigabytes', '21.9 gigabytes', '2.19 terabytes'], answer: 0, worked: '3 million times 200 bytes is 600 megabytes a day. Times 365 is about 219 gigabytes.' },
    { id: 'd51-p02', prompt: 'A backfill covers 100 million rows. One worker does 5000 rows a second. How long with 8 workers?', hint: 'Combined rate is workers times rate per worker.', options: ['5 and a half hours', '4 minutes', 'About 42 minutes'], answer: 2, worked: '8 times 5000 is 40 thousand rows a second. 100 million divided by 40 thousand is 2500 seconds, about 42 minutes.' },
  ],
  build: { id: 'd51-b01', title: 'Idempotent CSV loader', goal: 'Write a Python loader that validates a CSV and can be re-run without creating duplicates.', steps: ['Read the CSV and check the header matches the expected columns.', 'Reject rows with a missing id or unparseable timestamp and count them.', 'Write rows to SQLite with INSERT OR REPLACE keyed on id.', 'Print rows loaded, rows rejected and total rows in the table.'], done: 'Running the loader twice on the same file leaves the total row count unchanged.', check: { q: 'Why key the write on id rather than append?', options: ['Appending is slower', 'A re-run then replaces rather than duplicates', 'SQLite cannot append'], answer: 1, explain: 'Replacing on the key makes the load idempotent. Retries become safe.' } },
  incident: { id: 'd51-i01', title: 'Training table doubled overnight', signal: 'The daily training table has twice the usual row count. The model retrained on it and holdout accuracy fell three points.', clue: 'The scheduler log shows the load job ran twice after a timeout, and it uses plain INSERT.', options: ['Traffic genuinely doubled overnight', 'The retried job inserted every row twice', 'The holdout set is corrupted'], answer: 1, explain: 'A non-idempotent load was retried. Deduplicate on the event id, change the load to an upsert, and add a row count check against yesterday before any retrain.' },
  research: { id: 'd51-r01', question: 'Does validating data before training prevent more incidents than monitoring model outputs after deployment?', hypothesis: 'Input validation catches over half of the incidents that output monitoring would find, and days earlier.', method: 'Review the last 12 months of incidents. For each, record whether an input rule or an output alert would have fired first.', check: { q: 'Why review past incidents rather than run a new experiment?', options: ['Experiments are illegal', 'Real incidents show what actually goes wrong', 'Past data is always cleaner'], answer: 1, explain: 'Historical incidents are ground truth. A synthetic test may miss the real failure modes.' } },
  decision: { id: 'd51-l01', situation: 'A product team wants streaming features for the fraud model. The batch pipeline updates hourly. Streaming would take a team three months to build and run.', options: ['Build the streaming pipeline now', 'Cut batch to five minutes and measure the lift', 'Keep hourly and decline the request'], answer: 1, reasoning: 'Nobody has shown fresher features improve fraud catch rate. Five minute batches are cheap and test the claim. Build streaming only if the measured lift justifies three months.' },
};

export default day;
