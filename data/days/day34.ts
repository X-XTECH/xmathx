import type { Day } from '../types';

const day: Day = {
  day: 34,
  title: 'Operating systems',
  goal: 'Explain how the kernel isolates processes and where attackers try to break out.',
  concepts: [
    { id: 'd34-c01', term: 'Kernel', plain: 'The kernel is the core program that controls the hardware and every other program.', why: 'All security on a machine rests on the kernel. If it is compromised, nothing above it can be trusted.', example: 'Linux is a kernel. Ubuntu is the kernel plus thousands of programs around it.', check: { q: 'What does the kernel control?', options: ['Only the screen', 'Hardware and all other programs', 'Web pages'], answer: 1, explain: 'The kernel owns the hardware and hands out slices of it to programs.' } },
    { id: 'd34-c02', term: 'Process', plain: 'A process is one running program with its own memory and identity.', why: 'Every attack that runs code runs it as a process. Listing processes is the first step in any investigation.', example: 'Opening two terminal windows starts two shell processes with different process ids.', check: { q: 'What does a process have that a file does not?', options: ['A name', 'A place on disk', 'Running code and its own memory'], answer: 2, explain: 'A file is data at rest. A process is code executing with memory and a process id.' } },
    { id: 'd34-c03', term: 'Thread', plain: 'A thread is one line of execution inside a process, sharing its memory.', why: 'Threads make programs fast but share everything. One bad thread can corrupt the whole process.', example: 'A web server runs one process with 50 threads, each handling a request.', check: { q: 'What do threads in the same process share?', options: ['Nothing', 'A separate disk', 'Memory'], answer: 2, explain: 'Threads share the address space of their process, so any thread can read what another wrote.' } },
    { id: 'd34-c04', term: 'System call', plain: 'A system call is how a program asks the kernel to do something for it.', why: 'Opening files, sending packets and starting processes all go through system calls. They are the natural place to watch and filter.', example: 'Python open() ends in the openat system call to the kernel.', check: { q: 'Which of these needs a system call?', options: ['Adding two numbers', 'Writing to a file', 'A loop over a list'], answer: 1, explain: 'Files belong to the kernel. Arithmetic and loops stay inside the process.' } },
    { id: 'd34-c05', term: 'Kernel mode', plain: 'The processor runs in user mode for programs and kernel mode for the kernel.', why: 'User mode cannot touch hardware or other memory directly. This wall is what keeps one program from another.', example: 'A browser tab crashes in user mode. The rest of the machine keeps running.', check: { q: 'Which mode can access hardware directly?', options: ['Kernel mode', 'User mode', 'Both equally'], answer: 0, explain: 'Only kernel mode may touch hardware. User programs must ask through system calls.' } },
    { id: 'd34-c06', term: 'Virtual memory', plain: 'Each process sees its own private address space that the kernel maps onto real memory.', why: 'A process cannot read another process memory by accident or on purpose. Isolation starts here.', example: 'Two programs both use address 0x400000 but land on different physical pages.', check: { q: 'What does virtual memory give each process?', options: ['More disk space', 'Faster CPU', 'Its own private address space'], answer: 2, explain: 'The kernel maps each process to separate physical pages, so addresses do not collide.' } },
    { id: 'd34-c07', term: 'Page fault', plain: 'A page fault happens when a program touches memory that is not currently mapped.', why: 'Some faults are normal and load data. A flood of faults means memory is exhausted and the machine is thrashing.', example: 'The program reads a page that was swapped to disk. The kernel pauses it and loads the page.', check: { q: 'What does a high page fault rate usually mean?', options: ['The CPU is idle', 'The disk is full', 'Memory is short and swapping'], answer: 2, explain: 'When memory runs out, pages go to disk and every touch of them faults.' } },
    { id: 'd34-c08', term: 'File descriptor', plain: 'A file descriptor is a small number a process uses to name an open file or socket.', why: 'Sockets, pipes and files all look the same to a process. Leaked descriptors exhaust limits and open holes.', example: 'Descriptor 0 is input, 1 is output, 2 is errors. The next open file gets 3.', check: { q: 'Which descriptor number is standard output?', options: ['0', '1', '2'], answer: 1, explain: 'Zero is standard input, one is standard output, two is standard error.' } },
    { id: 'd34-c09', term: 'Scheduler', plain: 'The scheduler decides which process gets the CPU next and for how long.', why: 'A runaway process can starve everything else. Priorities and limits keep critical services responsive.', example: 'With four cores and forty runnable threads, each gets a short slice in turn.', check: { q: 'What does the scheduler decide?', options: ['Which process runs next', 'How much disk a file uses', 'Which port a service listens on'], answer: 0, explain: 'The scheduler shares CPU time between everything that wants to run.' } },
    { id: 'd34-c10', term: 'Context switch', plain: 'A context switch saves one process state and loads another so the CPU can change task.', why: 'Switches cost time. Too many threads or interrupts can spend more CPU switching than working.', example: 'A switch costs a few microseconds. 100,000 switches a second wastes a good share of one core.', check: { q: 'What is saved during a context switch?', options: ['The file system', 'The CPU registers and process state', 'The network cable'], answer: 1, explain: 'The kernel stores the registers and state so the process can resume exactly where it stopped.' } },
    { id: 'd34-c11', term: 'Interrupt', plain: 'An interrupt is a hardware signal that makes the CPU stop and run a kernel handler.', why: 'Keyboards, network cards and timers all interrupt. Interrupt storms are a classic denial of service symptom.', example: 'A packet arrives, the network card interrupts, the kernel copies the packet into memory.', check: { q: 'What raises an interrupt?', options: ['A hardware event', 'A Python function', 'A file name'], answer: 0, explain: 'Devices raise interrupts to get the kernel to handle something now.' } },
    { id: 'd34-c12', term: 'Daemon', plain: 'A daemon is a background process that starts at boot and runs without a user.', why: 'Web servers, databases and agents are daemons. An unexpected daemon is often persistence for an attacker.', example: 'sshd waits on port 22 from boot until shutdown.', check: { q: 'What is a daemon?', options: ['A kernel driver', 'A user login', 'A background service process'], answer: 2, explain: 'Daemons run in the background with no terminal, usually started by the init system.' } },
    { id: 'd34-c13', term: 'Root', plain: 'Root is the all-powerful user. Everything else should run with the least power it needs.', why: 'A bug in a root process is a bug in the whole machine. Dropping privileges limits blast radius.', example: 'The web server starts as root to bind port 80, then switches to user www-data.', check: { q: 'Why should services not run as root?', options: ['A compromise would own the whole machine', 'Root is slower', 'Root cannot open ports'], answer: 0, explain: 'A flaw in a root service hands the attacker everything. A low user limits the damage.' } },
    { id: 'd34-c14', term: 'Namespaces', plain: 'Namespaces give a process its own view of processes, network and file system.', why: 'Containers are built from namespaces. They isolate without a second kernel, so a kernel bug still escapes them.', example: 'Inside a container, process id 1 is your app, while the host sees it as process 48213.', check: { q: 'What do namespaces isolate?', options: ['A process view of system resources', 'The CPU speed', 'The kernel itself'], answer: 0, explain: 'Each namespace gives its members a private view of pids, network or mounts.' } },
    { id: 'd34-c15', term: 'Syscall filtering', plain: 'Seccomp lets a process declare which system calls it may make, and the kernel blocks the rest.', why: 'An exploit that cannot call execve or connect has few moves. Filtering turns a breach into a crash.', example: 'A model server allows read, write and mmap but blocks socket, so it can never phone home.', check: { q: 'What happens when a filtered process makes a blocked call?', options: ['The call is queued', 'The kernel refuses it or kills the process', 'The call is run as root'], answer: 1, explain: 'Seccomp returns an error or terminates the process, depending on the policy.' } },
  ],
  equations: [
    { id: 'd34-e01', name: 'CPU utilisation', symbol: 'U = b / T', tokens: [{ sym: 'U', word: 'utilisation' }, { sym: '=', word: 'is' }, { sym: 'b', word: 'busy-time' }, { sym: '/', word: 'divided by' }, { sym: 'T', word: 'total-time' }], literal: 'utilisation is busy-time divided by total-time', meaning: 'The share of time the CPU was working. Near 100 percent means queues build and latency climbs.', example: 'Busy 45 seconds out of 60. Utilisation is 0.75, or 75 percent.', check: { q: 'A core is busy 30 seconds in every 40. What is utilisation?', options: ['0.75', '0.3', '1.33'], answer: 0, explain: '30 divided by 40 is 0.75.' } },
    { id: 'd34-e02', name: 'Amdahl speed-up', symbol: 'S = 1 / ((1 − p) + p / n)', tokens: [{ sym: 'S', word: 'speed-up' }, { sym: '=', word: 'is' }, { sym: '1', word: 'one' }, { sym: '/', word: 'divided by' }, { sym: '−', word: 'minus' }, { sym: 'p', word: 'parallel-share' }, { sym: 'n', word: 'cores' }], literal: 'speed-up is one divided by, open bracket one minus parallel-share close bracket plus parallel-share divided by cores', meaning: 'Adding cores only speeds up the parallel part. The serial part caps the gain no matter how many cores you add.', example: 'Parallel-share 0.8, cores 4. One over 0.2 plus 0.2 is 2.5 times faster.', check: { q: 'Parallel share is 0.5. What is the best possible speed-up with infinite cores?', options: ['Unlimited', '2 times', '0.5 times'], answer: 1, explain: 'The serial half never shrinks. One over 0.5 is 2, however many cores you add.' } },
    { id: 'd34-e03', name: 'Page address', symbol: 'A = p·S + o', tokens: [{ sym: 'A', word: 'address' }, { sym: '=', word: 'is' }, { sym: 'p', word: 'page-number' }, { sym: '·', word: 'times' }, { sym: 'S', word: 'page-size' }, { sym: '+', word: 'plus' }, { sym: 'o', word: 'offset' }], literal: 'address is page-number times page-size plus offset', meaning: 'Memory is cut into fixed pages. Any address is a page number and a distance into that page.', example: 'Page 3, page-size 4096, offset 100. Address is 12,288 plus 100, which is 12,388.', check: { q: 'Page size 4096. Which page holds address 8200?', options: ['Page 1', 'Page 3', 'Page 2'], answer: 2, explain: '8200 divided by 4096 is 2 remainder 8. Pages start at 0, so it is page 2.' } },
    { id: 'd34-e04', name: 'Switch overhead', symbol: 'F = n·c / T', tokens: [{ sym: 'F', word: 'overhead-fraction' }, { sym: '=', word: 'is' }, { sym: 'n', word: 'switches' }, { sym: '·', word: 'times' }, { sym: 'c', word: 'switch-cost' }, { sym: '/', word: 'divided by' }, { sym: 'T', word: 'total-time' }], literal: 'overhead-fraction is switches times switch-cost divided by total-time', meaning: 'Every switch is time not spent on real work. Many short slices can eat a core.', example: '50,000 switches a second at 4 microseconds each. 0.2 seconds per second, so 20 percent overhead.', check: { q: 'Switch cost doubles and switches stay the same. What happens to overhead?', options: ['Halves', 'Unchanged', 'Doubles'], answer: 2, explain: 'Overhead is switches times cost, so doubling the cost doubles the overhead.' } },
    { id: 'd34-e05', name: 'Queue wait', symbol: 'W = 1 / (s − a)', tokens: [{ sym: 'W', word: 'average-wait' }, { sym: '=', word: 'is' }, { sym: '1', word: 'one' }, { sym: '/', word: 'divided by' }, { sym: 's', word: 'service-rate' }, { sym: '−', word: 'minus' }, { sym: 'a', word: 'arrival-rate' }], literal: 'average-wait is one divided by service-rate minus arrival-rate', meaning: 'A simple queue model. As arrivals approach the service rate the wait explodes, which is why a saturated server feels frozen.', example: 'Service-rate 100 per second, arrival-rate 90. Wait is one over 10, which is 0.1 seconds.', check: { q: 'Arrival rate rises to equal service rate. What happens to wait?', options: ['It falls to zero', 'It grows without limit', 'It stays at one second'], answer: 1, explain: 'The denominator becomes zero. The queue never drains and the wait keeps growing.' } },
  ],
  problems: [
    { id: 'd34-p01', prompt: 'A training script is 90 percent parallel. You move from 1 GPU to 8. What is the speed-up?', hint: 'Use Amdahl. Serial share is 0.1.', options: ['8 times', 'About 4.7 times', 'About 7.2 times'], answer: 1, worked: '0.1 plus 0.9 over 8 is 0.1 plus 0.1125, which is 0.2125. One over that is about 4.7.' },
    { id: 'd34-p02', prompt: 'A server handles 200 requests per second and receives 190 per second. What is the average wait from the queue model?', hint: 'One over service minus arrival.', options: ['0.1 seconds', '10 seconds', '0.005 seconds'], answer: 0, worked: '200 minus 190 is 10. One over 10 is 0.1 seconds. At 199 it would be 1 second, so headroom matters.' },
  ],
  build: { id: 'd34-b01', title: 'Root process auditor', goal: 'Write a Python script that lists processes running as root and flags any not on an allow list.', steps: ['Loop over numeric directories in /proc and read each status file.', 'Parse the Name and Uid lines. Keep entries where uid is 0.', 'Load an allow list of expected root process names from a file.', 'Print every root process not on the list with its pid and exe path.'], done: 'Running it shows expected daemons as fine and a test process as flagged.', check: { q: 'Why compare against an allow list rather than a block list?', options: ['Block lists are illegal', 'Unknown processes are caught, not just known bad', 'Allow lists use less memory'], answer: 1, explain: 'A block list only catches what you already know. An allow list surfaces anything new.' } },
  incident: { id: 'd34-i01', title: 'Unknown root process from /tmp', signal: 'A host shows a process named kworkerd using 95 percent CPU as root. It started at 03:10 and no deployment ran.', clue: 'Its executable path is /tmp/.cache/kworkerd, and real kernel workers have no file on disk.', options: ['A normal kernel worker thread', 'A cryptominer disguised as a kernel worker', 'A scheduled backup job'], answer: 1, explain: 'Kernel workers are named kworker and live in memory, not in /tmp. Isolate the host, capture the binary and memory, find how it was launched and rebuild.' },
  research: { id: 'd34-r01', question: 'Does seccomp filtering on an inference server cut exploitability without a measurable latency cost?', hypothesis: 'A strict allow list blocks 90 percent of known post-exploit payloads and adds under 1 percent latency.', method: 'Run the server with and without the filter. Replay a suite of benign requests and a set of known payloads. Compare blocked calls and p99 latency.', check: { q: 'What does the benign request suite protect against?', options: ['A filter that breaks normal use', 'Slow disks', 'Kernel upgrades'], answer: 0, explain: 'A filter that blocks a call the server needs would fail benign requests. The suite catches that.' } },
  decision: { id: 'd34-l01', situation: 'Researchers want root on shared GPU training nodes to install drivers and tools. The platform team refuses. Experiments are delayed by a week each time a package is missing.', options: ['Grant root to every researcher', 'Build images with drivers and allow sudo for named commands', 'Refuse all changes and keep the delays'], answer: 1, reasoning: 'Root for everyone turns one phished laptop into a cluster breach. A refusal costs research speed. Prepared images plus narrow sudo give speed and keep the blast radius small.' },
};

export default day;
